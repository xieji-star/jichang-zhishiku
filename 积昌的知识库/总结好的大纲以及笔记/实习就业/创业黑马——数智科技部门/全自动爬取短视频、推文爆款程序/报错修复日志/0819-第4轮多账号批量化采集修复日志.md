---
title: "0819-第4轮多账号批量化采集修复日志"
type: 报错修复日志
date: 2026-08-19
tags:
  - 报错修复
  - 第4轮
  - 多账号采集
  - 字幕直取
  - 永久日志
  - 续接交接
status: 进行中（禁止第5轮与生产执行）
source: Codex 多 Agent 第4轮修复会话（截至 2026-08-19）
---

# 0819-第4轮多账号批量化采集修复日志

> [!summary] Summary / 概要
> **EN:** Round 4 is **not complete**. Many bounded code changes have passed code-only gates, but production execution is still blocked by remaining lifecycle/evidence reviews, subtitle authorization/provenance, MediaCrawler commercial eligibility, unresolved bounded test failures, and the unexecuted 12-video comment-only remediation. Round 5, service restart, live collection, real canary, production ACL/Cookie migration, and production database mutation remain forbidden.
>
> **中文：** 第4轮**尚未全部完成**。多项有界代码修改已获得“仅代码 GO”，但生产执行仍受生命周期/证据复审、字幕授权与来源、MediaCrawler 商业资格、限定测试遗留失败，以及尚未执行的 12 条视频仅评论补救阻断。继续禁止第5轮、服务重启、真实采集、真实 canary（灰度验证）、生产 ACL/Cookie 迁移及生产数据库修改。

🔥 **<span style="color:#e74c3c">核心结论 / Core verdict：当前状态是 AT-RISK / NO-GO，不得宣称本轮已完成。</span>**

## 索引

- 🧭 [[#1. Current verdict and continuation entry / 当前结论与续接入口]] — 换号后先确认范围与项目路径
- 📊 [[#2. Immutable production baseline / 不可改写的生产基线]] — 第4轮原始证据与未执行补救
- 🛡️ [[#3. Permanent safety rules / 永久安全规则]] — 数据、日志、网络与生产禁令
- ✅ [[#4. Completed code-only gates / 已完成的仅代码门禁]] — 已通过复审的修复项
- 🧪 [[#5. Implemented and awaiting director re-gate / 已实现待代码总监复审]] — 最新但尚未独立放行的实现
- 🚧 [[#6. Remaining blockers and omissions / 剩余阻断与遗漏]] — P0/P1 与法律、授权问题
- 🧾 [[#7. Test evidence and unresolved 13-node baseline / 测试证据与13项待解释基线]] — 已通过数据及精确失败节点
- 👥 [[#8. Agent ownership and current work / Agent分工与当前工作]] — 三组与主 Agent 的续接职责
- 🪜 [[#9. Safe next actions in strict order / 严格顺序的安全下一步]] — 不得越级的迭代次序
- 📋 [[#10. Copy-ready prompt for the new account / 换号续接提示词]] — 新账号可直接复制使用
- 🔗 [[#11. Related evidence / 关联证据]] — 上一轮真实相关修复日志

## 1. Current verdict and continuation entry / 当前结论与续接入口

### 1.1 Project location / 项目位置

- **EN:** The real project root is `D:\全自动爬取短视频、推文爆款程序`.
- **中文：** 真实项目根目录为 `D:\全自动爬取短视频、推文爆款程序`。
- **EN:** The Codex shell may open in `C:\Users\asus\Documents\Codex\2026-08-18\agent-f-claude-agents`; this is not the project root. Always set the working directory explicitly before inspection or tests.
- **中文：** Codex Shell 可能打开在 `C:\Users\asus\Documents\Codex\2026-08-18\agent-f-claude-agents`，该目录不是项目根目录；检查或测试前必须显式切换工作目录。

### 1.2 Exact continuation status / 精确续接状态

- **EN:** Do not restart from scratch. Preserve the current dirty worktree and every existing user/Agent change.
- **中文：** 不要从头重做；必须保留当前工作树及所有用户/Agent 已有修改。
- **EN:** Continue bounded source review, focused isolated tests, and independent code-director gates only.
- **中文：** 只允许继续有界源码审查、隔离专项测试与独立代码总监门禁。
- **EN:** GitHub candidate search is closed at **0 qualified candidates**, **0 approved installs**, **0 approved five-run trials**. Do not restart the search unless the user opens a new objective.
- **中文：** GitHub 候选搜索已以**0 个合格候选、0 个批准安装、0 个批准五次试跑**结束；除非用户另开目标，否则不要重新搜索。

📌 **<span style="color:#2980b9">“CODE-ONLY GO”仅表示源码与隔离测试可保留，不等于允许生产运行。</span>**

## 2. Immutable production baseline / 不可改写的生产基线

### 2.1 Original Round 4 task / 第4轮原任务

| Field / 字段 | Preserved value / 永久保留值 |
|---|---|
| Task ID / 任务 ID | `12fa1f267316452e9c5b433d51b4c23b` |
| Terminal state / 终态 | `completed`, progress `100` |
| Videos / 视频 | `135` |
| Comments / 评论 | `2376` |
| Creator profiles / 达人资料 | `8` |
| Raw accounts succeeded / 原始成功账号计数 | `36`（历史漂移证据，不改写） |
| Raw accounts failed / 原始失败账号计数 | `1` |
| Transcription / 转录 | `129` succeeded, `12` failed, `6` classifier skips |
| Current task collection logs / 当前任务采集日志 | `2021` 条（全库永久历史总数更大，不混淆） |
| Error logs / 报错日志 | `200` 条（只读核验时） |
| Comment repair rows / 评论修复行 | `collection_comment_repairs = 0` |

- **EN:** The raw `result_json`, timestamps, task row, account rows, videos, comments, transcriptions, manifests, checkpoints, collection logs, and error logs are historical evidence. Never rewrite them to “look correct.”
- **中文：** 原始 `result_json`、时间戳、任务行、账号行、视频、评论、转录、manifest、checkpoint、采集日志与报错日志均为历史证据，禁止为了“看起来正确”而改写。
- **EN:** The read model is designed to derive corrected counters from durable rows and append-only reconciliation; the original raw result must remain unchanged.
- **中文：** 读模型应从持久行与追加式对账派生修正计数，原始结果必须保持不变。

### 2.2 Exact incomplete remediation / 精确未完成补救

- `小Lin说` manifest contains `15` members.
- Only `3` members have nonzero comment checkpoints, totaling `60` comments.
- The intended remediation is a separate, explicitly bound `comment_only` task for the exact missing `12` video IDs.
- The repair table is still empty; **the live repair has never been run**.
- The code-level read overlay was tested for `2376 + 240 = 2616`, but this is an expected post-repair view, not current live production truth.

📊 **<span style="color:#e67e22">当前真实决定性证据：`collection_comment_repairs = 0`，12 条精确评论补采尚未执行。</span>**

## 3. Permanent safety rules / 永久安全规则

⚠️ **<span style="color:#e74c3c">报错日志与采集日志必须全部永久保留，不得删除、截断、轮转覆盖、重置或原地恢复。</span>**

### 3.1 Data and evidence / 数据与证据

- Never run destructive reset/restore utilities against live databases.
- Never execute the legacy backup `restore.ps1` that uses `Copy-Item -Force` over live SQLite files.
- `scripts/batch-data-cycle.py` reset/restore entry points must remain fail-closed; backup/report-only behavior may remain.
- Never delete expired/malformed transcript evidence. Expiry is a read filter only.
- Future error/collection log writes must be centrally redacted before persistence; historical rows must never be rewritten.
- Unique immutable log files, append-only manifests, hashes, byte counts, fsync, full-chain validation, and create-exclusive creation are required.

### 3.2 Operations / 操作禁令

- No production database mutation or live repair yet.
- No service restart, Round 5, live collection, production browser/Cookie use, phone/device registration, signer activation, APP flag enablement, or real network canary.
- No production ACL migration until code gates pass and the user explicitly authorizes the controlled migration.
- No delete/overwrite of production logs, reports, backups, evidence, or Cookie canonical data.
- No clone/install/connect of unapproved GitHub projects.

🔥 **<span style="color:#e74c3c">任何“为了测试方便而清库、覆盖日志、恢复旧数据库”的做法都违反永久保留规则。</span>**

## 4. Completed code-only gates / 已完成的仅代码门禁

### 4.1 Retention and logging / 保留与日志

- Service-launch logging: unique create-exclusive stdout/stderr, redaction-before-disk, append-only hash-linked manifest, child reap, byte/hash evidence; director GO for retention in tree.
- Database error logs: future writes centrally redacted; append-only behavior tested; historical rows untouched.
- Immutable capture shared layer: bounded streaming redaction, arbitrary Netscape Cookie values and Bearer/Basic protection, cross-process full-chain manifest, retryable idempotent close, private bounded linkage; code-only GO. Operational ACL migration remains separate.
- Subtitle success evidence: append-only redacted JSONL, full-chain validation, fsync, legacy file preserved, display-only 100-row view; code-only GO.

### 4.2 Collection correctness / 采集正确性

- `comment_only` fail-open path is closed before account/provider work. Missing/mismatched manifest, wrong original task, invalid membership, or unsupported gap reason now fails closed and cannot become a full recrawl.
- Request budgets classify real Douyin endpoints, fail closed on unknown endpoints, reject `notdouyin.com`, propagate budget exceptions, and avoid false completion.
- Restart defer refuses unsafe/unsealed process ownership; live collection process uses sealed process boundaries.
- API defaults to strict serial account order. The current limiter is truthfully named account-start pacing, not global per-request limiting. Parallel `>1` fails before pool/account start while per-request enforcement is unverified.

### 4.3 Durable cache and lifecycle / 持久缓存与生命周期

- `SQLiteTranscriptCache` uses append-only revision rows with UUID/hash; UPDATE/DELETE triggers reject mutation on legacy and revision tables. `get()` treats expired/malformed entries as non-mutating misses; `health()` is SELECT-only after schema initialization.
- PyVideoTrans lifecycle: sealed Windows Job/POSIX group boundary, bind-before-work, whole-tree pause/resume/termination verification, no resume on cancel, primary error preservation, independent terminal evidence; code-only GO.
- Creator profile lifecycle: sealed boundary, immutable dual streams, output/JSON/profile validation before `completed`, real Windows no-orphan evidence; code-only GO.
- APP subtitle worker lifecycle: bounded sanitized output, truthful outcomes, whole-tree teardown, no raw transcript persistence; code-only GO, while APP activation remains NO-GO.

### 4.4 Platform routing and performance / 平台路由与性能

- DouyinMcp deterministic capability circuit is task-scoped, single-flight, and shares the exact production route with health reporting.
- Structural result: `8` work items changed from `8` setup launches to `1` launch + `7` short-circuits, an `87.5%` setup-subprocess reduction, with actual route-2 fallback integration coverage.
- Non-route1 process-scoped cooldown/history survives task reset; repaired route1 can recover in the next task.
- ASR preflight is shared/single-flight only within a task; `8` concurrent preflights become `1` GET (`87.5%` structural reduction). Failures are not cached; next task resets.
- Cookie vault mechanics: dedicated owner+SYSTEM vault, reparse rejection, identity-bound cleanup, atomic isolated per-run copies, cross-process locking, canonical unchanged; code-only GO. Production migration is not authorized.

### 4.5 Test isolation and deployment truth / 测试隔离与投产真实性

- Test environment redirection happens before application imports.
- Session-finish/unconfigure sentinels detect production mutation even after collection/import failures.
- Sentinels cover production DB/WAL/journal files, accounts, Cookie/evidence files, top-level `logs/`, `runtime/logs/`, `runtime/reports/`, and `MediaCrawler/database/media_crawler.db`, with bounded streaming fingerprints and reparse rejection.
- MediaCrawler deployment eligibility is default-false and requires a reviewed authorization document hash. The reviewed hash set is empty; start/resume fail before mutation and health degrades truthfully.

💡 **<span style="color:#2980b9">优化原则：修复报错时同步审计相邻生命周期、证据完整性、失败语义、风控真实性与性能，避免只补报错点。</span>**

## 5. Implemented and awaiting director re-gate / 已实现待代码总监复审

### 5.1 SocialKit inspect / SocialKit 检查线路

- Sealed process boundary, bind-before-pump, unconditional verified whole-tree teardown.
- Immutable redacted stdout/stderr; bounded `1 MiB` in-memory raw parse only.
- Output validation before `completed`; exact outcome/primary outcome/return code/duration/tree linkage.
- Primary error preserved with independent terminal-failure evidence.
- Evidence: `py_compile` PASS; focused `10 passed / 9 deselected`; isolated integration `26 passed`.
- Status: **implemented, awaiting independent code-director re-gate**.

### 5.2 Active YtDlpCliProvider / 活跃 yt-dlp 下载线路

- Suspended/isolated launch, boundary binding before pumps, immutable redacted dual streams, unconditional whole-tree verification.
- Output-file validation before `completed`; terminal-failure evidence for coexistence failures.
- URL is hash-linked; query/log secrets are redacted.
- Isolated Cookie copy covers the full lifecycle and is removed on success, timeout, nonzero exit, and launch exception; canonical file remains unchanged.
- Evidence: `py_compile` PASS; focused `9 passed / 49 deselected`; plugin routes `58 passed`; secure-cookie + boundary `16 passed / 1 skipped`.
- F2 remains disabled because Cookie-on-argv is unsafe.
- Status: **implemented, awaiting independent code-director re-gate**.

### 5.3 Subtitle host/readiness and resource bounds / 字幕域名就绪与资源边界

- Central `app_subtitle_transport_ready()` is now required for initial, ranking, download, redirect, and final URL paths.
- Nonempty host configuration with `verified=False` proves zero Request/open/download calls.
- Canonical and legacy IPv4 literals such as `127.1`, `0177.0.0.1`, and `0x7f.1` are rejected.
- Bounds include `8 MiB` response, JSON depth `32`, container count `20,000`, candidates `128`, source `64`, stickers `32`, URL-list `64`, URL length `8192`, language `64`, known format `32`, IDs `128`, strict int64 expiry, strict boolean auto flag, and maximum `3` unique outbound URLs.
- End-to-end signer-response hardening additionally enforces 2xx integer status, JSON media type, exact bounded body length, duplicate-key rejection, exact signer URL/header schema, and sanitized type/code-only telemetry.
- Evidence: focused `115/115`; adjacent offline `196/196`; compile/invariants PASS.
- Production trust remains `frozenset()`, verification remains `False`, APP remains OFF.
- Status: **implemented, awaiting independent code-director re-gate**.

### 5.4 PyVideoTrans preflight and ffprobe diagnostics / PyVideoTrans预检与ffprobe诊断

- Shared captured-tool lifecycle now provides sealed launch/bind, immutable redacted dual streams, `1 MiB` bounded in-memory stdout, semantic validation before `completed`, whole-tree teardown verification, and independent finalization evidence.
- Python 3.10/CLI preflight and ffprobe return code, JSON, and duration validation occur inside the evidence phase; only successful preflight is cached.
- Evidence: new diagnostics `8 passed`; combined transcription + diagnostics + process boundary `50 passed`; `py_compile` PASS.
- Status: **implemented, awaiting independent code-director re-gate**.

📊 **<span style="color:#e67e22">以上三项只有离线/隔离证据，没有真实抖音网络成功率与时延证据。</span>**

## 6. Remaining blockers and omissions / 剩余阻断与遗漏

### 6.1 P0 blockers / P0 阻断

- Actual exact-12 `comment_only` production repair has not run.
- SocialKit, active yt-dlp, and latest subtitle host/resource changes still need independent director re-gates.
- Active local-ASR ffmpeg compaction/segmentation in `asr_service.py::_run_ffmpeg` still uses `subprocess.run(capture_output=True)`, discards full streams, and lacks whole-tree teardown proof; it needs the same sealed captured-tool evidence primitive.
- Production immutable-log/subtitle-evidence/Cookie ACL migrations have not been authorized or verified on disk.
- MediaCrawler bundled license is `NON-COMMERCIAL LEARNING LICENSE 1.1`; commercial/large-scale use needs written authorization or a reviewed permissive replacement. Current authorization hash set is empty.
- APP v40 direct transport has no approved numeric-license artifact, no reproducible authorized constants provenance, no defensible exact subtitle CDN hostname, no legal authorization, and no approved real canary.
- Official Douyin OpenAPI exposes no native-caption endpoint for arbitrary public bloggers. Volcengine VOD ASR is an output substitute only for media the operator is authorized to upload; it is not native-caption direct.

### 6.2 P1 backlog / P1 待办

- Route2 “75-second total budget” is not yet a proven hard wall-clock budget; remaining deadline must reach probes/providers and sealed subprocess waits.
- Future authorized deployment probe should recheck reparse ancestors after descriptor read and convert `OSError`/`TimeoutExpired` to degraded health rather than HTTP 500.
- Some immutable manifests still have nonblocking strict-schema/global-ID/check-open/performance hardening backlog.
- Process-group numeric reuse and evidence distinction between tree verification and leader wait failure remain nonblocking hardening items.
- Candidate/attempt telemetry must remain explicitly labeled to avoid confusing semantic variants with unique outbound requests.

⚠️ **<span style="color:#e74c3c">可信字幕主机集合为空不是“缺配置”，而是当前无法证明域名来源时必须保持的安全失败关闭状态。</span>**

## 7. Test evidence and unresolved 13-node baseline / 测试证据与13项待解释基线

### 7.1 Broad-suite rule / 大范围测试规则

- Do not rerun the full backend/TestClient suite until isolation gates and bounded targets are explicitly reviewed.
- The earlier phrase “11 pre-existing failures” is unsupported because the original report/node IDs were not retained. Do not repeat that claim.
- A bounded command over `backend/tests/test_collection.py backend/tests/test_multi_account.py` produced an exact baseline of `13 failed / 57 passed` before the latest local isolation-test fixes.

### 7.2 Exact 13 failed nodes / 精确13项失败节点

1. `test_provider_stage_report_keeps_current_account_progress`
2. `test_provider_waits_for_manual_handoff_before_persisting_awaiting_state`
3. `test_demo_task_completes_without_fake_videos`
4. `test_collection_reports_rejected_sync_rows_truthfully`
5. `test_one_transcription_failure_does_not_stop_following_videos`
6. `test_resume_partitions_done_and_failed_accounts`
7. `test_recovery_omits_user_skipped_account`
8. `test_one_account_crawler_failure_does_not_stop_following_accounts`
9. `test_task_is_failed_when_every_account_fails`
10. `test_real_mediacrawler_zero_video_result_cannot_complete`
11. `test_selected_video_is_processed_only_for_its_actual_author`
12. `test_runner_reuses_profile_within_route_and_isolates_next_route`
13. `test_runner_fails_closed_when_all_clone_profiles_are_unready`

### 7.3 Diagnosis already made / 已完成诊断

- Two provider-constructor tests used stale fakes; signature-aware option filtering was added and their targeted rerun passed.
- Two multi-account tests assumed clone mode while strict serial is now the safe default; tests now explicitly opt into verified parallel mode and passed targeted reruns.
- Several async tests patched task/log operations to a temporary database but left result increment/reconciliation on a different default database, producing `no such table: collection_tasks`. A test helper was added to route task-result state to the temporary database; targeted/full bounded rerun still needs completion and evidence recording.
- Partition/recovery failures still require evidence-based diagnosis if they remain after isolation fixes.

📌 **<span style="color:#e67e22">下一账号不得把这13项直接称为“产品回归”；必须先完成同一隔离环境下的精确复跑与分类。</span>**

## 8. Agent ownership and current work / Agent分工与当前工作

| Owner / 负责人 | Current responsibility / 当前职责 | Safety boundary / 安全边界 |
|---|---|---|
| Primary Agent / 主 Agent | 13项限定测试隔离与复跑、协调门禁、最终生产放行判断 | 不跑全量、不改生产 |
| Platform optimization group / 平台优化组 | SocialKit、yt-dlp 后续总监复审；PyVideoTrans preflight/ffprobe 永久诊断；继续审计相邻可优化点 | 不真实运行、不启用F2 |
| Subtitle-direct R&D group / 字幕直取研发组 | 最新 host/readiness/resource 复审；继续离线 signer→parser 失败开放/泄密/资源审计 | APP OFF、无 canary/网络 |
| Independent code director / 独立代码总监 | 对每个稳定差异做只读独立门禁，阻止虚假完成与范围偏移 | 不替代实现、不授权生产 |

### 8.1 GitHub group disposition / GitHub搜索组去向

- Search stopped after exhaustive review found zero qualified projects.
- `yt-dlp` is parser/reference/Web fallback only; it is not Douyin v40 APP native-caption transport.
- No candidate may be cloned, installed, connected, or trialed five times under the current goal.
- Search capacity was reassigned to platform optimization.

💡 **<span style="color:#2980b9">字幕直取是核心组件，研发继续；但研发继续不等于降低来源、法律、凭据、进程、日志或网络门禁。</span>**

## 9. Safe next actions in strict order / 严格顺序的安全下一步

1. **EN:** Finish and independently re-gate SocialKit, active yt-dlp, and latest subtitle host/resource deltas.
   **中文：** 完成并独立复审 SocialKit、活跃 yt-dlp 与最新字幕域名/资源边界增量。
2. **EN:** Close PyVideoTrans preflight/ffprobe and every remaining reachable subprocess diagnostic-retention gap.
   **中文：** 收口 PyVideoTrans 预检/ffprobe 及其余可达子进程诊断保留缺口。
3. **EN:** Run only the exact bounded collection/multi-account tests under the corrected sandbox; record node-by-node status and root cause. Do not run the broad backend suite.
   **中文：** 只在修正后的沙箱中复跑精确限定测试，逐节点记录状态与根因；不要运行全量 backend。
4. **EN:** Re-gate the hard Route2 deadline and any newly found adjacent maintainability/performance issue without increasing concurrency or risk.
   **中文：** 复审 Route2 硬时限及新发现的相邻可维护性/性能问题，不得提高并发或风控暴露。
5. **EN:** Keep APP direct disabled until provenance, license/legal authorization, exact trusted host, operational ACL migration, and approved real-canary prerequisites all exist.
   **中文：** 在来源、许可证/法律授权、精确可信域名、生产 ACL 迁移及获批真实 canary 条件全部具备前，APP 直取保持关闭。
6. **EN:** Only after every active lifecycle/evidence P0 is green, create a separate remediation task explicitly bound to original task `12fa1f267316452e9c5b433d51b4c23b`, remediation type `comment_only`, and the exact 12 missing video IDs.
   **中文：** 只有所有活动生命周期/证据 P0 全绿后，才创建独立补救任务，显式绑定原任务、`comment_only` 类型与精确12条缺失视频 ID。
7. **EN:** During repair, preserve the original task and all rows/logs; no detail/profile/list/media/video/ASR fallthrough; reconcile only through append-only repair evidence and the read model.
   **中文：** 补救时保留原任务与所有行/日志；禁止详情、资料、列表、媒体、视频、ASR 回退；仅通过追加式修复证据与读模型对账。
8. **EN:** After repair succeeds and evidence is independently reviewed, decide whether Round 4 can close. Round 5 requires a separate explicit authorization.
   **中文：** 补救成功且证据独立复审后，再判断第4轮能否结束；第5轮必须另行明确授权。

🔥 **<span style="color:#e74c3c">严禁跳过门禁直接执行12条生产补采；“修复代码已写好”不等于“生产执行已获批”。</span>**

## 10. Copy-ready prompt for the new account / 换号续接提示词

> [!important] Copy everything below into the new Codex task / 将下方全部复制到新的 Codex 任务
>
> **EN:** Continue the unfinished Round 4 work for project `D:\全自动爬取短视频、推文爆款程序`. First read `F:\积昌的知识库 - 副本\总结好的大纲以及笔记\实习就业\全自动爬取短视频、推文爆款程序\报错修复日志\0819-第4轮多账号批量化采集修复日志.md` completely. Do not restart from scratch and do not revert the dirty worktree. Keep all production data, collection logs, error logs, reports, backups, Cookie evidence, manifests, and historical rows permanently; never delete, overwrite, truncate, rotate away, reset, or restore them in place. No production DB mutation, live collection, service restart, Round 5, APP enablement, phone/device registration, production Cookie/ACL migration, or real network canary without my explicit authorization. Wake three groups: platform optimization, subtitle-direct R&D, and independent code director. Work in strict order: (1) re-gate SocialKit, active yt-dlp, and latest subtitle host/resource changes; (2) close PyVideoTrans preflight/ffprobe and remaining subprocess evidence gaps; (3) finish the exact bounded 13-test isolation rerun and classify every node; (4) close Route2 hard-deadline and adjacent code-quality gaps; (5) keep APP OFF until provenance/legal/license/trusted-host/ACL/authorized-canary gates pass; (6) only when every lifecycle/evidence P0 is green, create a separate exact-12-ID `comment_only` remediation bound to original task `12fa1f267316452e9c5b433d51b4c23b`. Preserve the original raw result and reconcile through append-only evidence/read model only. Use immediate source review + py_compile + focused tests whenever `/simplify` is unavailable. Report all claims as code-only/offline/structural/live accurately.
>
> **中文：** 继续项目 `D:\全自动爬取短视频、推文爆款程序` 尚未完成的第4轮工作。先完整阅读 `F:\积昌的知识库 - 副本\总结好的大纲以及笔记\实习就业\全自动爬取短视频、推文爆款程序\报错修复日志\0819-第4轮多账号批量化采集修复日志.md`。不要从头开始，不要回退当前脏工作树。永久保留全部生产数据、采集日志、报错日志、报告、备份、Cookie证据、manifest及历史行，禁止删除、覆盖、截断、轮转丢失、重置或原地恢复。未经我明确授权，不得修改生产DB、真实采集、重启服务、进入第5轮、启用APP、注册手机/设备、迁移生产Cookie/ACL或执行真实网络canary。唤醒平台优化、字幕直取研发、独立代码总监三组。严格按顺序推进：（1）复审SocialKit、活跃yt-dlp及最新字幕域名/资源改动；（2）收口PyVideoTrans预检/ffprobe与剩余子进程证据缺口；（3）完成精确13项限定测试隔离复跑并逐项分类；（4）收口Route2硬时限与相邻代码质量问题；（5）来源/法律/许可证/可信域名/ACL/获批canary全部通过前保持APP关闭；（6）只有全部生命周期/证据P0全绿后，才创建显式绑定原任务 `12fa1f267316452e9c5b433d51b4c23b` 的精确12-ID `comment_only` 补救任务。原始结果保持不变，只通过追加式证据与读模型对账。若 `/simplify` 不可用，每次改动后立即以源码复查、`py_compile` 和专项测试替代。所有结论必须准确标注为仅代码、离线、结构或真实运行证据。

## 11. Related evidence / 关联证据

- [[0815-多账号采集与缺链接报错修复]] — Earlier multi-account creator-link and path-format failures; this note continues the same platform repair history without rewriting the earlier evidence. / 前序多账号主页链接与路径格式故障记录；本笔记续接同一平台修复历史，不改写旧证据。

### 11.1 Evidence wording rules / 证据表述规则

- “Passed” must include the exact test scope and count.
- “GO” must state whether it is code-only, operational, or production.
- Structural request reduction is not a live speedup claim.
- Offline parser tests are not proof of working APP transport.
- Zero-network and fail-closed evidence are valid safety results, not subtitle success-rate evidence.
- `/simplify` was unavailable in this environment; the accepted substitute after each code change was immediate source review, `py_compile`, and focused isolated tests.

📌 **<span style="color:#2980b9">本文档用于换号续接，是当前修复状态的导航，不授权任何生产写入或真实试跑。</span>**

---

## 12. 2026-08-20 relay recovery snapshot / 2026-08-20 接力恢复快照

> [!important] Superseding status / 替代状态
>
> **EN:** This section supersedes earlier pending/failed code-gate statements where they conflict. All active Round 4 code, lifecycle, and evidence P0 gates are now **code-only/offline GREEN**. Round 4 overall and production remain **NO-GO**.
>
> **中文：** 如与前文的待审/失败代码门禁状态冲突，以本节为准。第4轮所有当前可达的代码、生命周期与证据 P0 现均为**仅代码/离线 GREEN**；第4轮整体及生产仍为 **NO-GO**。

### 12.1 Completed recovery gates / 已完成恢复门禁

| Area / 模块 | Verified result / 已核验结果 | Scope / 证据范围 |
|---|---:|---|
| Exact collection baseline / 精确采集基线 | `71/71 passed` | `test_collection.py + test_multi_account.py`; replaces the preserved `13 failed / 57 passed` baseline / 替代原保留的13失败/57通过基线 |
| ASR and ffmpeg lifecycle / ASR与ffmpeg生命周期 | `24/24 passed` | Sealed tree, immutable dual streams, validation before success / 密封整树、双流不可变日志、成功前校验 |
| SocialKit health and inspect / SocialKit健康与检查 | code-only GREEN / 仅代码绿灯 | Independent selected gate `21 passed / 9 deselected` / 独立限定门禁 |
| Subtitle-direct / 字幕直取 | independent `141/141`; adjacent `204/204` | APP remains OFF; trust remains empty/unverified; zero network / APP关闭、信任为空且未验证、零联网 |
| Risk-control probe / 风控探针 | `41/41 passed` | Exact Boolean schema, sealed tree, immutable diagnostics / 精确布尔结构、密封整树、不可变诊断 |
| Runtime storage probe / 容量探针 | `21/21 passed` | Sealed robocopy lifecycle and validation-before-success / robocopy密封生命周期与成功前校验 |
| MediaCrawler authorization gate / MediaCrawler授权门禁 | `5/5 passed` | Stable identity/hash/reparse checks; reviewed hash set still empty / 稳定身份、哈希、重解析检查；审查哈希仍为空 |
| Route2 hard deadline / Route2硬截止 | final decisive `4/4`; frozen secure gate `16 passed / 1 skipped` | Deadline reaches probe/provider/read/Cookie lock; exact-attempt artifacts only / 截止贯通探针、Provider、读取与Cookie锁；仅接受本次精确产物 |
| Neutral captured-tool layer / 中性捕获工具层 | independent `60/60 passed` | No circular import; error identity, bounded parse, verified teardown, append-only terminal evidence retained / 无循环依赖；保留异常身份、有界解析、整树核验与追加式终态证据 |

### 12.2 Frozen source hashes / 冻结源码哈希

- `backend/app/captured_tool.py`: `48AD286BD836412B6DE24FF4545D3C5C2220E58F2FBFEE2F9CF06F6B19136071`
- `backend/app/services/transcription.py`: `B79455A7B0E3A8AA94D01ABD16698C80D6ADBE9B08131A58441C4B4E084468FB`
- `backend/app/services/asr_service.py`: `60D5E302B887F697E03F80720B552741A32130A5480DD99689CA2BD72132F185`
- `backend/app/services/socialkit_resolver.py`: `757EDC62E36A4F0CF1FB41BC969CC1A0CB7DB684E4CDB260FFF35B227EB686B7`
- `backend/app/risk_control.py`: `EEDFCFEDF364C9BF311E818A33588E3184C3A1BC40CC53C79CF24B5986B2711C`
- `backend/app/services/runtime.py`: `B4D968DEA8770178E3C49F3066D440107B162BDE820EF99055D190B995EAD6BE`
- `backend/app/services/plugin_routes.py`: `301FFEF14E039EB3CA616D605F0A6B1E662B9A209B467CDDC98764906963415A`
- `backend/app/services/secure_cookie_files.py`: `CF4638D8792677B140187B8117882F6CA262E6C7ABA7C6674265ABCC3717F735`
- `MediaCrawler/media_platform/douyin/app_subtitles.py`: `3E54F91231EF96EE9CEA411C91F56DEB0E8E9E495E976815AED765241D9B7922`
- `MediaCrawler/media_platform/douyin/subtitles.py`: `A880FD5207F09DF91B8E8EB1EDE52C1CB95B1AEEA07A39EF8AD989AA85F030E4`
- Subtitle provider wrapper / 字幕Provider封装: `BBE2CC7A1E6C2C5EE472218B6881C807F529864B777B0CA81790EF9F1C8F4AD6`

### 12.3 Material fixes completed after the original handoff / 原交接后完成的关键修复

- **EN:** Recovery now fails closed before provider/browser work when the account ledger or required component/job evidence is unreadable; it cannot silently become a full recrawl.
  **中文：** 恢复任务在账号账本或必要组件/job证据不可读时，会在Provider/浏览器工作前失败关闭，不能静默退化为完整重采。
- **EN:** yt-dlp seals evidence only after identity-bound Cookie-copy cleanup; simultaneous provider/cleanup errors retain the primary error plus independent terminal evidence. F2 is code-level fail-closed even if an executable and Cookie are configured.
  **中文：** yt-dlp 仅在身份绑定的Cookie副本清理后封存证据；Provider与清理错误并存时保留主错误及独立终态证据。F2即便配置可执行文件与Cookie，也在代码层失败关闭。
- **EN:** Route2 uses one caller deadline through probes, provider health/download, HTTP body reads, yt-dlp waits, and Cookie-lock acquisition. Evil0ctal no longer scans output trees and can accept only its exact bounded attempt artifact.
  **中文：** Route2用同一调用方截止时间贯通探针、Provider健康/下载、HTTP正文读取、yt-dlp等待与Cookie锁获取。Evil0ctal不再扫描输出树，只能接受本次有界尝试的精确产物。
- **EN:** Current reachable SocialKit, risk-control, ASR/ffmpeg, and storage-maintenance subprocesses now use the shared sealed captured-tool layer with permanent redacted stdout/stderr evidence and verified whole-tree teardown.
  **中文：** 当前可达的SocialKit、风控、ASR/ffmpeg及容量维护子进程现统一使用共享密封捕获工具层，永久保留脱敏stdout/stderr并核验整棵进程树清理。
- **EN:** Subtitle entry points and private helpers gate before Request/opener construction; telemetry hashes account/video references and excludes raw subtitle/Cookie content. Retained-evidence audit found no defensible exact production caption hostname.
  **中文：** 字幕公开入口与私有helper均在Request/opener构造前门禁；遥测哈希化账号/视频引用，不记录原始字幕/Cookie。留存证据审计未找到可辩护的精确生产字幕域名。

### 12.4 Remaining production blockers / 剩余生产阻断

1. **EN:** The exact-12 `comment_only` production remediation is still unexecuted and has not been explicitly authorized. The original task and every historical row/log must remain unchanged.
   **中文：** 精确12条 `comment_only` 生产补救仍未执行且未获明确授权；原任务及全部历史行/日志必须保持不变。
2. **EN:** APP trust remains empty/unverified. No exact caption host, authorized numeric-license provenance, legal/license approval, production ACL migration, or approved real canary exists.
   **中文：** APP信任仍为空且未验证；缺少精确字幕域名、授权数字许可证来源、法律/许可证批准、生产ACL迁移及获批真实canary。
3. **EN:** APK→native binding is reproducible, but installed signer key/Gorgon/prefix/version values are not derivable by the retained updater. The unused manual 43-field protobuf map diverges from the runtime Argus serializer and is an explicit activation blocker.
   **中文：** APK→native绑定可复现，但已安装签名key/Gorgon/前缀/版本值无法由留存更新器推导；未使用的手工43字段protobuf映射与运行时Argus序列化器分歧，是明确启用阻断。
4. **EN:** MediaCrawler reviewed authorization hashes remain empty and its bundled license remains non-commercial-learning-only; commercial deployment is not authorized.
   **中文：** MediaCrawler审查授权哈希仍为空，内置许可证仍仅限非商业学习；商业投产未获授权。
5. **EN:** Production immutable-log/Cookie ACL migration and on-disk readback verification remain unperformed. The future authorized external MediaCrawler module probe must be sealed before any reviewed authorization hash is added.
   **中文：** 生产不可变日志/Cookie ACL迁移及磁盘读回核验尚未执行；未来获授权的MediaCrawler外部模块探针必须在加入任何审查授权哈希前完成密封。
6. **EN:** No live collection, live performance, production operability, restart, or full-backend-suite evidence was produced in this relay. These were intentionally prohibited.
   **中文：** 本次接力未产生真实采集、真实性能、生产可运行性、重启或全量backend套件证据；这些操作均被刻意禁止。

🔥 **<span style="color:#e74c3c">Current handoff point: code-only Round 4 gates are green, but production remains NO-GO. Do not execute the exact-12 repair, APP canary, ACL migration, restart, or Round 5 without a new explicit authorization.</span>**

🔥 **<span style="color:#e74c3c">当前交接点：第4轮代码门禁已绿，但生产仍为NO-GO。未经新的明确授权，禁止执行精确12条补救、APP canary、ACL迁移、重启或第5轮。</span>**

## 13. Continuation / 后续接力

- [[0820-字幕直取&连接采集的开发日志]] — **EN:** Continues the Subtitle Direct production gate, evidence-lock work, GitHub candidate decisions, and the new video-only multi-link collection design. **中文：** 续接字幕直取投产门禁、证据锁改造、GitHub候选裁定及新增仅视频多链接采集设计。
