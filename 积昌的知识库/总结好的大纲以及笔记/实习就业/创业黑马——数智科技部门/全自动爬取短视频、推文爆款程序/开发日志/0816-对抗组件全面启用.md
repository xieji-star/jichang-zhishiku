---
title: 0816 对抗组件全面启用
type: 开发日志
date: 2026-08-16
tags:
  - 对抗组件
  - 用户决策
  - 代理轮换
  - NopeCHA
  - patchright
---

> [!summary] 2026-08-16 用户决策（风险自担）
> 用户于 2026-08-16 明确决策：突破项目原合规红线，①类对抗组件全部投入使用，接受账号风控/法律风险。原"隐身/指纹/代理轮换保持禁用"红线作废，改以用户 2026-08-16 决策为准。本文件如实记录该决策（标注"2026-08-16 用户决策"）、各组件启用状态与后续启用步骤。

## 背景

- 0815 风控组件以"合规闸门式适配层"接入生产，NopeCHA/隐身指纹/代理轮换默认全部禁用（见 [[0815-风控组件接入生产]]）。
- 2026-08-16 用户决策：突破原合规红线，①类对抗组件全部投入使用，风险自担。

## 组件启用状态（2026-08-16）

| 组件 | 状态 | 说明 |
|------|------|------|
| curl_cffi | ✅ 启用 | Chrome TLS 指纹请求，复用 call_with_policy（限速+指数退避） |
| NopeCHA | ✅ 接线待 Key | 自动验证码求解已接线，待填入 API Key 后生效 |
| patchright | ✅ 接线待浏览器下载 | 无检测浏览器已接线，待下载浏览器内核后生效 |
| 代理轮换 | ⚠️ 开关已开，待代理源 | `RISK_PROXY_ENABLED=true` 已写入 `.env.local`；真正轮换需 `RISK_PROXY_POOL_URL` 指向运行中的 proxy_pool 服务（Redis+代理源），此依赖未就绪；proxy_preflight（脏 IP 拒绝→回退直连）已接线 |
| cloak | ⏳ 待后续 | 未列入本轮启用范围，待后续评估 |
| nodriver | ⏳ 待后续 | 未列入本轮启用范围，待后续评估 |
| haipproxy | ⏳ 待后续 | 代理源方案之一，与 proxy_pool 的选型统一后评估 |
| Botright | ⏳ 待后续 | 未列入本轮启用范围，待后续评估 |
| fingerprintjs | ⏳ 待后续 | 指纹方案需与 patchright 路线统一后评估 |

## 启用步骤清单

1. [x] `.env.local` 写入 `RISK_PROXY_ENABLED=true`（2026-08-16 完成）
2. [ ] 启动 proxy_pool 服务（Redis + 代理源），配置 `RISK_PROXY_POOL_URL` 指向它
3. [ ] 验证代理预检路径：脏 IP 拒绝 → 回退直连（proxy_preflight 已接线）
4. [ ] 填入 NopeCHA API Key，验证验证码求解链路
5. [ ] 下载 patchright 浏览器内核，验证无检测启动
6. [ ] 端到端采集验证 + 观察账号风控信号（账号级熔断器持续统计）
7. [ ] 持续关注账号风控/法律风险，必要时回退开关

## 风险声明

> [!warning] 风险自担
> 用户 2026-08-16 决策明确接受账号风控/法律风险。对抗组件可能违反平台服务条款，使用不当可能导致账号封禁、IP 封锁或法律纠纷；启用前请确认合规责任归属，运行期间持续监控风控信号。

## 相关笔记

- [[0815-风控组件接入生产]]
- [[如何解决人机验证问题]]
- [[多worker并发池设计思路]]

---

## 十轮测试终审记录（2026-08-16 晚）

> [!success] 代码总监终审：10 轮测试全部通过，B 验收 5/5，成功率全程 100%，正式收官。

| 轮次 | 场景 | pytest | 采集 | 成功率 |
|------|------|--------|------|--------|
| 1 | 基线回归 | 279/2s | — | — |
| 2 | Edge CDP（登录态+5次尝试闭环） | 279/2s | completed 160s | 100% |
| 3 | Edge CDP 复测 | 279/2s | completed 161s | 100% |
| 4 | Edge CDP 第三连（基线 3/3 钉死） | 279/2s | completed 150s | 100% |
| 5 | patchright 单变量 | 279/2s | completed 160s | 100% |
| 6 | patchright 复测（三重证据） | 279/2s | completed 160s | 100% |
| 7 | 代理池单变量（preflight 真实执行，安全回退直连） | 279/2s | completed 150s | 100% |
| 8 | 全组件共存（7 开关全开） | 279/2s | completed 140s | 100% |
| 9 | 双账号串行（错峰 54s 实证，videos 2→4） | 279/2s | completed 291s | 2/2=100% |
| 10 | 全量回归 + 终审 | 279/2s | completed 160s | 100% |

**全程指标**：十轮 pytest 零回归；风控信号 argus=2（第 2 轮被预算+重试消化）、blocked=0、429=0、403=0、verification=0——十轮真实采集**零账号封锁**。

**组件终态**：4 投产（代理池/CloakBrowser/nodriver 旁路/fingerprintjs 自测页）+ 2 判不适用（Botright/haipproxy，已回滚+理由记录）+ NopeCHA 待用户填 Key（守卫路径已验证）。新增服务全部只监听 127.0.0.1 回环，默认路由行为未变，既有链路零破坏。

**遗留开放项**：① NopeCHA Key 未配；② 代理 HTTPS 隧道缺失（正效果待 HTTPS 代理源）；③ 探针总预算 deadline 未设；④ NoneType.user_data_dir 既有已知项；⑤ 熔断器全流程仅单测覆盖（可选：注入式故障演练）。
---

## 组件删除记录（2026-08-16 晚，用户决策）

> [!note] 用户指令：删除『要花钱或无法稳定投产』的组件，释放硬盘空间。

| 组件 | 处理 | 释放空间 |
|------|------|----------|
| NopeCHA（付费打码，无 Key） | 已从 MediaCrawler venv 与 security venv 卸载、wheel 已删、开关 RISK_NOPECHA_ENABLED=false、manifest 标记 removed | ~5MB+ |
| 代理池运行时（缺 HTTPS 源无法正投产） | 三服务已停、proxy_pool 专属 venv 已删、开关 RISK_PROXY_ENABLED=false、RISK_PROXY_POOL_URL 已移除（源码目录保留，重启即恢复） | ~50MB |
| nodriver（仅独立旁路，未接线） | 已从 security venv 卸载、wheel 已删、manifest 标记 removed | ~10MB |
| Botright / haipproxy | 确认无残留（此前已回滚） | — |
| 高质量付费代理源 | 从未安装，无需处理 | — |

**保留投产的组件**：熔断器、随机错峰、全局限速、指数退避、curl_cffi、mss 截图、patchright、CloakBrowser、fingerprintjs 自测页（全部免费且已实测可用）。

**验证**：删除后后端重启正常，/api/risk-control/health 开关：CIRCUIT_BREAKER=true / CURL_CFFI=true / STAGGER=true / EVIDENCE_SCREENSHOT=true / PROXY=false / NOPECHA=false / BROWSER_ROUTE=patchright；verify.ps1 与 plugin-manifest.json 已同步更新并通过语法校验。