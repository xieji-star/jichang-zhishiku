---
title: "0821凌晨-开发日志 / 0821 Early-Morning Development Handoff"
aliases:
  - "0821凌晨开发交接"
  - "Round 5 Harness SelfTest Handoff"
type: "开发日志 / Development Handoff"
date: 2026-08-21
created: "2026-08-21 09:54:06 +08:00"
updated: "2026-08-21 10:02:45 +08:00"
status: "round5-red-current-selftest-not-run"
project: "全自动爬取短视频、推文爆款程序"
tags:
  - 开发日志
  - 换号接力
  - 平台优化
  - 链接采集
  - 字幕直取冻结
  - 前端改版
  - Round-5
  - SelfTest
  - 生产门禁
source: "[[0820下午-开发日志]] and the current Codex multi-Agent relay state / [[0820下午-开发日志]] 与当前 Codex 多 Agent 接力状态"
cssclasses:
  - development-handoff
---

# 0821凌晨-开发日志 / 0821 Early-Morning Development Handoff

> [!summary] Binding summary / 约束性概要
> **EN:** This is a relay continuation of [[0820下午-开发日志]], not a new plan. Subtitle Direct development is deliberately frozen and all available subtitle capacity was reassigned to Platform Optimization. Link Collection R1-R4C/P2 and the two-file runtime race repair retain their exact frozen source identities. The detached Round-5 harness advanced through five permitted SelfTest attempts; the latest completed attempt made Python GREEN but stopped RED in the Node guard because numeric-loopback `dns.lookup('127.0.0.1')` was denied. A bounded Node DNS correction has since received an independent Code Director **static GREEN for exactly one fresh complete SelfTest only**, but that corrected candidate has not been executed and has no current permit.
>
> **中文：** 这是 [[0820下午-开发日志]] 的接力续篇，不是新规划。字幕直取研发已明确冻结，原字幕组的全部可用产能已转入平台优化。链接采集 R1-R4C/P2 以及两文件运行时竞态修复继续保持精确冻结身份。第5轮外置门禁已进行五次获许可的 SelfTest；最近一次已让 Python 自检 GREEN，但 Node 守卫因拒绝数值回环 `dns.lookup('127.0.0.1')` 而保持 RED。随后完成的 Node DNS 有界修正已获得独立代码总监“**仅允许一次全新完整 SelfTest**”的静态 GREEN，但该修正候选尚未执行，当前也没有有效许可。
>
> 🔥 **<span style="color:#e74c3c">Binding verdict / 约束性结论：Round 5 remains RED. The corrected current candidate is unexecuted; production, live/manual testing, real URLs, real credentials, and simultaneous multi-account collection remain NO-GO. / 第5轮继续为 RED。当前修正候选尚未执行；生产、真实/手工测试、真实链接、真实凭据及多账号并发采集全部继续 NO-GO。</span>**

> [!note] Trigger timing / 触发时间说明
> **EN:** The scheduled trigger was Beijing time 01:45. The F: knowledge-base volume was unavailable during the interrupted turn; this finalized note was written only after the user mounted F: again and it became readable at execution time. No project or harness phase was run while producing this note.
>
> **中文：** 计划触发时间为北京时间 01:45。中断期间 F: 知识库卷不可用；用户重新接入 F 盘并在执行时确认可读后，才完成本文。撰写本文期间未运行任何项目测试或门禁阶段。

## Related notes / 相关笔记

- [[0820下午-开发日志]] — **EN:** The direct predecessor containing the Round-5 long-path mirror result, Link runtime race repair, and the earlier frontend freeze claims. **中文：** 直接上一份交接，记录第5轮长路径镜像结果、Link 运行时竞态修复及此前的前端冻结声明。
- [[0823交接文档-Codex-Codex]] — **EN:** The next self-contained Codex handoff with the scoped manual authorization and the two terminal Round-5 user-test results. **中文：** 下一份自包含Codex交接，包含范围手测授权及两次第5轮用户测试终态。

## Index / 索引

- 🧭 [[#1. Binding status at execution time / 执行时约束状态]] — Current readiness and operational verdict / 当前就绪度与操作裁定
- 🔄 [[#2. Changes since the prior handoff / 相对上一交接的变化]] — Superseded facts and new evidence / 已被替代事实与新增证据
- 👥 [[#3. Group recovery and capacity allocation / 小组恢复与产能分配]] — Completed work, frozen scopes, and active roles / 完成项、冻结范围与活动角色
- 🎙️ [[#4. Subtitle Direct frozen state / 字幕直取冻结状态]] — Preserved evidence and production-off boundary / 保留证据与生产关闭边界
- 🔗 [[#5. Link Collection R1-R4C and P2 / 链接采集R1-R4C与P2]] — Layer status, ownership, and exact hashes / 分层状态、归属与精确哈希
- 🎨 [[#6. Frontend redesign truth / 前端改版真实状态]] — Navigation, collection, video assets, and provenance conflict / 导航、采集、视频资产与来源冲突
- 🧪 [[#7. Round-5 chronological attempts / 第5轮按时间顺序的尝试]] — Five permitted SelfTests and exact failure causes / 五次获许可自检与精确失败原因
- 🧊 [[#8. Retained evidence and recoverability / 保留证据与可恢复性]] — Current run directories, seals, and missing historical mirror / 当前运行目录、封印与缺失旧镜像
- 🔐 [[#9. Current five-file candidate identity / 当前五文件候选身份]] — Exact bytes, hashes, closure, and candidate / 精确字节、哈希、闭包与候选
- 🌐 [[#10. Latest Node DNS correction / 最新Node DNS修正]] — Allowed loopback, denied DNS, and SelfTest expectations / 允许回环、拒绝DNS与自检预期
- 🛡️ [[#11. Code Director verdict and permit state / 代码总监裁定与许可状态]] — One-run static GREEN and consumed permits / 单次静态GREEN与已消耗许可
- 🧩 [[#12. Platform defects versus harness-only defects / 平台缺陷与仅门禁缺陷]] — Truthful causal separation / 真实因果分离
- 🗺️ [[#13. Ownership and freeze map / 所有权与冻结图]] — Disjoint edit boundaries for the next account / 下一账号互斥编辑边界
- 🚧 [[#14. Open blockers and unresolved questions / 当前阻断与未决问题]] — Required closures before any release / 任何放行前必须关闭的问题
- ⛔ [[#15. Manual production and multi-account boundary / 手工生产与多账号边界]] — Allowed review and forbidden operations / 允许审查与禁止操作
- 🪜 [[#16. Exact next-account resume sequence / 下一账号精确续接顺序]] — Shortest compliant continuation path / 最短合规续接路径
- ♻️ [[#17. Do not repeat / 禁止重复劳动]] — Frozen work and failed approaches / 已冻结工作与失败做法
- 📖 [[#18. Glossary / 名词解释]] — Plain-language gate terminology / 门禁术语白话解释

## 1. Binding status at execution time / 执行时约束状态

### English

- 🔥 **<span style="color:#e74c3c">Overall platform: not authorized for production and not authorized to use production as Round-5 testing.</span>**
- 📊 **<span style="color:#e67e22">Latest completed current-series SelfTest: Python GREEN, Node RED; the corrected successor candidate has never run.</span>**
- 💡 **<span style="color:#2980b9">Current Director approval is a static code-review verdict for one SelfTest, not a test result and not a production release.</span>**
- **Subtitle Direct:** code-only evidence is preserved, development is frozen, production flags remain OFF, and its former Agents are assigned to Platform work.
- **Link Collection:** R1-R4C/P2 and the runtime race repair remain frozen at their named identities; there is no new live-production authorization.
- **Frontend:** navigation, video assets, and the collection page are not globally frozen. The collection CSS identity conflicts with the prior handoff and must be treated as UNFROZEN.
- **Live Agent view at document execution:** the root coordinator and this documentation-only worker were the only live entries. The Platform harness implementation worker and independent Director had completed their latest bounded turns; no technical worker was executing a test.

### 中文

- 🔥 **<span style="color:#e74c3c">整个平台尚未获准投产，也未获准用生产环境完成第5轮测试。</span>**
- 📊 **<span style="color:#e67e22">当前系列最近一次已完成 SelfTest：Python GREEN、Node RED；其修正后的后继候选从未运行。</span>**
- 💡 **<span style="color:#2980b9">代码总监当前批准只是针对一次 SelfTest 的静态代码审查结论，不是测试结果，更不是生产放行。</span>**
- **字幕直取：** 仅代码证据继续保留，研发已冻结，生产开关保持 OFF，原字幕组 Agent 已转入平台工作。
- **链接采集：** R1-R4C/P2 与运行时竞态修复继续按指定身份冻结，没有新增真实生产授权。
- **前端：** 导航、视频资产与采集页均未形成整体冻结。采集 CSS 身份与上一交接冲突，必须按 UNFROZEN 处理。
- **文档执行时的实时 Agent 视图：** 只有主协调 Agent 和本文档 Worker 为活动条目。平台门禁实现 Worker 与独立代码总监已完成最近一次有界任务；没有技术 Worker 正在运行测试。

## 2. Changes since the prior handoff / 相对上一交接的变化

The predecessor remains valid historical evidence for its timestamp, but the following items were superseded. This note preserves the timeline instead of rewriting old evidence.

[[0820下午-开发日志]] 对其生成时点的描述仍是历史证据，但以下项目已被后续事实替代。本文不改写旧证据，只明确时间线。

| Change / 变化 | Current English truth | 当前中文事实 |
|---|---|---|
| Harness architecture / 门禁架构 | A short-path detached runner, Python audit guard, Node network guard, frozen contract, and failed-64 contract now exist under `C:\Users\asus\r5h`. | `C:\Users\asus\r5h` 下现已有短路径独立 runner、Python 审计守卫、Node 网络守卫、冻结契约及 failed-64 契约。 |
| Source closure / 源码闭包 | The corrected closure is 337 files with digest `B40BDE9A96BD35D89E6D067D60AA5D4CAB2303C5853BA0C426A6039267CFD301`. | 修正后的闭包为 337 个文件，摘要为 `B40BDE9A96BD35D89E6D067D60AA5D4CAB2303C5853BA0C426A6039267CFD301`。 |
| SelfTest progression / 自检推进 | Five old physical permits were used or expired across two pre-mirror failures and three retained mirror runs. | 五份旧物理许可已在两次镜像前失败和三次保留镜像运行中使用或过期。 |
| Latest completed run / 最近完成运行 | Python reached GREEN; Node stopped before listener setup because numeric-loopback DNS lookup was denied. | Python 已达到 GREEN；Node 因数值回环 DNS lookup 被拒绝而在监听器建立前停止。 |
| Current candidate / 当前候选 | The Node DNS correction changed the five-file candidate to `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2`; no run exists for it. | Node DNS 修正将五文件候选更新为 `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2`；该候选没有运行记录。 |
| Frontend collection CSS / 前端采集CSS | Prior log claimed `9E9E5974...`; execution-time bytes hash to `9808B2E6...`. Provenance is unresolved, so the old freeze claim is not current. | 旧日志声明 `9E9E5974...`；执行时字节哈希为 `9808B2E6...`。来源尚未解决，因此旧冻结声明已不能代表当前状态。 |
| Production gate / 生产门禁 | Still NO-GO; later harness progress did not release manual, live, or multi-account production. | 继续 NO-GO；后续门禁推进没有放行手工、真实或多账号生产。 |

📌 **<span style="color:#e67e22">Every GREEN or RED verdict applies only to the exact bytes, candidate identity, phase, permit, and evidence path named with it.</span>**

📌 **<span style="color:#e67e22">任何 GREEN 或 RED 只适用于与其同时指明的精确字节、候选身份、阶段、许可和证据路径。</span>**

## 3. Group recovery and capacity allocation / 小组恢复与产能分配

| Group / 小组 | Completed or preserved / 已完成或保留 | Current assignment / 当前分配 | Binding boundary / 约束边界 |
|---|---|---|---|
| Platform Optimization / 平台优化迭代组 | Implemented the detached harness chain, corrected source closure, fixed Python audit handling, and completed the bounded Node DNS correction. / 已实现外置门禁链、修正源码闭包、修复 Python 审计处理并完成有界 Node DNS 修正。 | Await one newly permitted SelfTest of the exact current candidate; no execution is authorized by this document. / 等待针对精确当前候选的新许可 SelfTest；本文不授权执行。 | No project behavior changes, no phase escalation, and no production. / 禁止修改项目行为、禁止阶段跃迁、禁止生产。 |
| Subtitle Direct / 字幕直取研发组 | Code-only GREEN evidence and default-OFF safeguards are preserved. / 仅代码 GREEN 证据与默认 OFF 防护已保留。 | All available Agents were reassigned to Platform Optimization; subtitle feature work is paused. / 全部可用 Agent 已转入平台优化；字幕功能研发暂停。 | Subtitle sources are frozen/no-touch and all runtime/production switches remain OFF. / 字幕源码冻结且禁止触碰，运行时/生产开关全部保持 OFF。 |
| Link Collection / 链接采集小组 | R1-R4C/P2 plus the runtime close-race repair retain exact source identities. / R1-R4C/P2 及运行时 close 竞态修复保留精确源码身份。 | No new feature work; support only evidence review after the harness gate. / 不新增功能；仅在门禁后支持证据审查。 | No real URLs, no creator-profile expansion, no live multi-account run. / 禁止真实链接、禁止扩展达人资料、禁止真实多账号运行。 |
| Independent Code Director / 独立代码总监 | Issued static GREEN for the bounded Node DNS correction and exactly one future complete SelfTest. / 已对有界 Node DNS 修正及未来精确一次完整 SelfTest 给出静态 GREEN。 | Must issue a new physical permit before that run and review its sealed evidence afterward. / 运行前必须签发新的物理许可，运行后复审封印证据。 | Approval does not extend to Collect64, Preflight64, Full, manual, or production phases. / 批准不覆盖 Collect64、Preflight64、Full、手工或生产阶段。 |
| Documentation worker / 文档Worker | Read the required skill, Function 4 example, prior handoff, current files, permits, and retained summaries. / 已读取指定 skill、功能四范例、上一交接、当前文件、许可及保留摘要。 | Owns only this note and the minimal reciprocal backlink. / 仅负责本文及最小反向链接。 | No project/harness/evidence edits and no test execution. / 禁止修改项目、门禁或证据，禁止运行测试。 |

💡 **<span style="color:#2980b9">“Relay mode” means continuing from exact frozen identities and retained evidence, not re-planning R1-R4C or reopening Subtitle Direct.</span>**

💡 **<span style="color:#2980b9">“接力模式”是从精确冻结身份与保留证据继续，不是重做 R1-R4C，也不是重新开启字幕直取。</span>**

## 4. Subtitle Direct frozen state / 字幕直取冻结状态

### 4.1 Preserved completion / 保留完成项

- **EN:** Central application/trust admission still occurs before network construction; parsing enforces trusted final host, exact integer 2xx, strict declared-length/received-byte agreement, UTF-8, bounded candidates, and fail-closed quality checks.
  **中文：** 中央应用/信任准入仍位于网络构造前；解析强制可信最终域名、精确整数 2xx、声明长度与接收字节严格一致、UTF-8、有界候选与失败关闭质量检查。
- **EN:** Neutral quality rejection falls through to ASR (automatic speech recognition, converting audio to text) without poisoning provider circuits.
  **中文：** 中性质量拒绝会回退 ASR（自动语音识别，即把音频转换为文本），且不污染 Provider 熔断状态。
- **EN:** A live default-OFF master switch blocks both direct providers and the MediaCrawler child path before acquisition/construction.
  **中文：** 实时默认 OFF 总开关会在获取/构造前同时阻断直取 Provider 与 MediaCrawler 子路径。
- **EN:** Success logging remains minimized to a 12-character SHA-256 reference plus `title_present`; raw aweme IDs and titles are not emitted.
  **中文：** 成功日志继续最小化为 12 位 SHA-256 引用及 `title_present`；不输出原始 aweme ID 与标题。

### 4.2 Exact preserved identities / 精确保留身份

| File or evidence / 文件或证据 | Exact SHA-256 or result / 精确SHA-256或结果 |
|---|---|
| `MediaCrawler/media_platform/douyin/app_subtitles.py` | `3E54F91231EF96EE9CEA411C91F56DEB0E8E9E495E976815AED765241D9B7922` |
| `MediaCrawler/media_platform/douyin/subtitles.py` | `A880FD5207F09DF91B8E8EB1EDE52C1CB95B1AEEA07A39EF8AD989AA85F030E4` |
| `backend/app/services/douyin_subtitles.py` | `39F93649E4FD2164806C59EDD76D2EA6F735360F862602ED3CD0DD0048E514A8` |
| App/provider focused / APP与Provider专项 | `141/141` |
| Adjacent subtitle suite / 相邻字幕套件 | `204/204` |
| Quality gate / 质量闸门 | `24/24` |
| Health/master integration / 健康与总开关 | `92` disjoint / 互不重复 |
| Logging minimization / 日志最小化 | `2/2` |

### 4.3 Frozen and OFF / 冻结且关闭

- 🔥 **<span style="color:#e74c3c">Subtitle Direct source ownership is frozen/no-touch; the group’s capacity is assigned to Platform Optimization.</span>**
- 🔥 **<span style="color:#e74c3c">字幕直取源码归属为冻结/禁止触碰；该组产能已分配给平台优化。</span>**
- **EN:** Exact caption-body trust host, signer provenance/constants, Argus serializer truth, legal/license authorization, protected identity/ACL evidence, authorized canary, and rollback evidence remain absent.
  **中文：** 精确字幕正文信任域名、签名来源/常量、Argus 序列化真实性、法律/许可证授权、受保护身份/ACL 证据、获批 canary 与回滚证据仍缺失。
- **EN:** Do not enable APP Subtitle or Subtitle Direct from code-only evidence.
  **中文：** 不得依据仅代码证据启用 APP Subtitle 或 Subtitle Direct。

## 5. Link Collection R1-R4C and P2 / 链接采集R1-R4C与P2

### 5.1 Product invariant / 产品不变量

- **EN:** One task may accept multiple links, normally from different creators, but collects only video-scoped data.
  **中文：** 一个任务可接收多条通常来自不同达人的链接，但只采集视频级数据。
- **EN:** Creator profiles, account pages, comments, avatars, search/hot lists, collections, and homepages remain forbidden.
  **中文：** 达人资料、账号页、评论、头像、搜索/热榜、合集与主页仍禁止采集。
- **EN:** The dedicated request/runner must not reuse `selected_video_ids` or creator-oriented flows.
  **中文：** 独立 request/runner 不得复用 `selected_video_ids` 或达人导向流程。
- **EN:** GitHub review authorized zero new installations and zero new dependencies; do not repeat repository installation screening.
  **中文：** GitHub 审查批准新增安装 0 个、新增依赖 0 个；不要重复仓库安装筛选。

### 5.2 Layer status and evidence / 分层状态与证据

| Layer / 层 | Binding state / 约束状态 | Named evidence / 指定证据 |
|---|---|---|
| R1 models/navigation/minimal UI / 模型、导航、最小UI | Code-only GREEN, frozen / 仅代码GREEN、冻结 | Backend `39/39`; frontend `9/9`; type-check/build PASS / 后端39/39、前端9/9、类型检查/构建通过 |
| R2 URL/resolver/deadline/cancel / URL、解析、截止、取消 | GREEN, frozen / GREEN、冻结 | Link + SocialKit `101/101` |
| R3 exact-once coordinator / 精确一次协调器 | GREEN, frozen / GREEN、冻结 | `68/68` |
| P2 sentinel-only regression / 仅哨兵回归 | GREEN file identity / GREEN文件身份 | Test hash retained below / 测试哈希见下表 |
| R4A DB/admission/replay / 数据库、准入、重放 | Code-only GREEN / 仅代码GREEN | Combined `157/157`; critical `58/58` ×3 / 综合157/157、关键58/58三次 |
| R4B persistent Store adapter / 持久Store适配器 | Code-only GREEN / 仅代码GREEN | Focused `83/83`; cross-layer `300/300` / 专项83/83、跨层300/300 |
| R4C original API/runtime / 原API与运行时 | Superseded historical GREEN / 被替代的历史GREEN | Focused `39/39` ×5; cross-layer `366/366` / 专项39/39五次、跨层366/366 |
| R4C runtime close-race repair / 运行时close竞态修复 | Current two-file frozen repair / 当前两文件冻结修复 | Compile; original close; deterministic adversarial; `26/26`; API/runtime `64/64` / 编译、原close、确定性对抗、26/26、API/runtime 64/64 |

### 5.3 Exact source identities / 精确源码身份

| Layer / 层 | File / 文件 | SHA-256 |
|---|---|---|
| R1 | `backend/app/models.py` | `482614EBE3E5307D5B677970258A9209116587FA236F3564A2D6D381C9320ADC` |
| R1 test / R1测试 | `backend/tests/test_link_collection_models.py` | `2BCAD2532D08E170CCE8F6EA1F478D4AF8ED40FDD4D6FCCB8F7E56EC736A11E5` |
| R2 | `backend/app/services/link_collection.py` | `12C37032D9033F7C383C6451BF50BA1210CDD45D7B075470EE8FC194D6DEE0C4` |
| R2 test / R2测试 | `backend/tests/test_link_collection.py` | `D1D5C9876C7DF3166F0CEAF30C96D43585A4BDE8136D01191E937CCC9A75536C` |
| R3 | `backend/app/services/link_collection_service.py` | `A057A4BDC4586A965BD43B8D12143D6194DCA02915DE766E1FC213E2586BD868` |
| R3/P2 test / R3与P2测试 | `backend/tests/test_link_collection_service.py` | `C276890FD153F7790D98BB24BF150C264FEB5F3509E14C8A6503A1A7D66E0C4B` |
| R4A | `backend/app/db.py` | `B99678FC2D78BB758BC5A13DE4712305831EE94747320A8EEEE27FF4CDFACA86` |
| R4A test / R4A测试 | `backend/tests/test_database.py` | `5E948DC2CFE724C9387C9BC1B0C58C5A5E7CD125F99A35D63BBB5E12295FD853` |
| R4B | `backend/app/services/link_collection_store.py` | `0E27CBE1EC811C4D72A505100D5FC47E4CA4E1B4E708D6B179854A7E27FDE779` |
| R4B test / R4B测试 | `backend/tests/test_link_collection_store.py` | `7357303A302CFD2C46CCAC00B55A86362794EA9D955CDC1BE09134E167B5DA29` |
| R4C API | `backend/app/main.py` | `7F1B5F71363ECB7909923D5D57497D285989A7D6FE627F55F97C8C131FCE6767` |
| R4C repaired runtime / 修复运行时 | `backend/app/services/link_collection_runtime.py` | `A50F85F52BE2B140B1AB575AA75F6EB89277C5D23CEB213EF2758DF9EA88EA7B` |
| R4C repaired test / 修复测试 | `backend/tests/test_link_collection_api_runtime.py` | `4B26D8B48DB4EF09ADA8726F06FA6D433E1AF0C0E23AA0B3BD6A0CD9581F3786` |

### 5.4 Runtime repair truth / 运行时修复事实

- **EN:** The genuine platform defect was a same-runtime collision between worker Store polling and the `close()` pause CAS (compare-and-swap, changing state only when the stored prior state exactly matches). Both used fail-fast `_link_connect(timeout=0)` with `BEGIN IMMEDIATE`, which could surface `store_ambiguous` during owned-executor close.
  **中文：** 真实平台缺陷是同一 runtime 内 worker Store 轮询与 `close()` 暂停 CAS（比较并交换，即仅在已存旧状态精确匹配时改状态）的碰撞。两者都使用失败即返的 `_link_connect(timeout=0)` 与 `BEGIN IMMEDIATE`，可在归属 executor 的 close 中暴露 `store_ambiguous`。
- **EN:** One local lock now serializes same-runtime worker Store calls and the close pause CAS. No retry was added; external database contention remains fail-fast; injected executor semantics remain unchanged.
  **中文：** 现用一个本地锁串行化同一 runtime 的 worker Store 调用与 close 暂停 CAS。没有增加重试；外部数据库竞争继续失败即返；注入 executor 语义不变。
- 🔥 **<span style="color:#e74c3c">Focused Link evidence does not authorize Link production; the complete release and external production gates are still open.</span>**
- 🔥 **<span style="color:#e74c3c">Link 专项证据不等于 Link 投产授权；完整发布门禁与外部生产门禁仍未关闭。</span>**

## 6. Frontend redesign truth / 前端改版真实状态

### 6.1 Collection and spoken-script page / 采集与口播页面

- **EN:** The screenshot-led collection workflow had stylelint and TypeScript GREEN intermediate checks in the previous turn.
  **中文：** 按截图推进的采集工作流在上一轮取得 Stylelint 与 TypeScript 绿色中间检查点。
- **EN:** The predecessor declared `collection-workflow.css` frozen at `9E9E59742B4777C8E330ACD8064B90A735ACFBDEE0DFEA9B5951BE184BD5DEE9`.
  **中文：** 上一交接声明 `collection-workflow.css` 冻结于 `9E9E59742B4777C8E330ACD8064B90A735ACFBDEE0DFEA9B5951BE184BD5DEE9`。
- **EN:** Execution-time read-only recomputation found 34,727 bytes and SHA-256 `9808B2E6DA332FF59A6CDE3F817FCF1C180446F93D8F6E15A487AE044CD815F3`.
  **中文：** 执行时只读重算得到 34,727 字节，SHA-256 为 `9808B2E6DA332FF59A6CDE3F817FCF1C180446F93D8F6E15A487AE044CD815F3`。
- ⚠️ **<span style="color:#e67e22">Because the bytes differ and no authoritative provenance/final review accompanies the new hash, the collection CSS is UNFROZEN and no-touch until ownership is resolved.</span>**
- ⚠️ **<span style="color:#e67e22">由于字节不同且新哈希没有权威来源/最终复审，采集 CSS 必须标为 UNFROZEN，并在归属解决前禁止触碰。</span>**

### 6.2 Navigation/sidebar / 导航栏与侧栏

- **EN:** The layout moved toward a left vertical grid plus sticky sidebar to reserve content width and avoid the prior fixed-overlay overlap at normal zoom.
  **中文：** 布局已朝左侧竖向 grid 加 sticky 侧栏推进，为主体预留宽度，避免此前 fixed 覆盖在正常缩放下重叠。
- **EN:** Focused navigation, type-check, stylelint, and build checkpoints were reported GREEN.
  **中文：** 导航专项、类型检查、Stylelint 与构建中间检查点曾报告 GREEN。
- **EN:** No final binding hash or one coherent supported-width visual review exists; status remains INTEGRATION/UNFROZEN.
  **中文：** 尚无最终约束性哈希，也没有覆盖支持宽度的一次统一视觉复审；状态继续为 INTEGRATION/UNFROZEN。

### 6.3 Video assets / 视频资产

- **EN:** Responsive analytics/detail layout, ARIA accessibility (web attributes that help assistive technologies understand controls), compact mode, dark-mode corrections, and detail interactions were implemented with intermediate green checks.
  **中文：** 已实现响应式分析/详情布局、ARIA 无障碍（帮助辅助技术理解控件的网页属性）、紧凑模式、暗色修正与详情交互，并有绿色中间检查点。
- **EN:** No complete video-assets binding hash or final screenshot-fidelity review exists; status remains INTEGRATION/UNFROZEN.
  **中文：** 尚无完整视频资产约束哈希或最终截图还原度复审；状态继续为 INTEGRATION/UNFROZEN。

### 6.4 Binding frontend conclusion / 前端约束结论

🔥 **<span style="color:#e74c3c">Do not claim that the whole frontend is complete or pixel-identical. Normal-scale non-overlap, responsive behavior, visual fidelity, and provenance still require a single coherent freeze review.</span>**

🔥 **<span style="color:#e74c3c">不得宣称整个前端已经完成或逐像素一致。正常缩放无重叠、响应式行为、视觉还原度与来源仍需一次统一冻结复审。</span>**

## 7. Round-5 chronological attempts / 第5轮按时间顺序的尝试

### 7.1 Historical baseline before the current harness series / 当前门禁系列前的历史基线

- **EN:** The earlier exact in-place gate compiled successfully, then the exact nine backend files returned `505 passed / 1 failed`; the isolated long-path mirror later returned `467 passed / 64 failed / 4 warnings` in 69.04 seconds, with frontend not run.
  **中文：** 较早的精确原地门禁编译通过，随后精确 9 个后端文件得到 `505 passed / 1 failed`；之后长路径隔离镜像得到 `467 passed / 64 failed / 4 warnings`、耗时 69.04 秒，前端未运行。
- **EN:** The one backend failure exposed the genuine Link runtime close race described above. All 64 long-path mirror failures were classified as harness-only: 23 long-path SocialKit, 31 blanket-socket/TestClient, and 10 synthetic-account fixture mismatch.
  **中文：** 那 1 个后端失败暴露了前述真实 Link runtime close 竞态。长路径镜像的 64 项失败全部归因为门禁：23 项 SocialKit 长路径、31 项全局 socket/TestClient、10 项合成账号 fixture 不匹配。

### 7.2 Attempt 1 — pre-mirror schema failure / 第1次——镜像前结构失败

| Field / 字段 | Exact value / 精确值 |
|---|---|
| Permit / 许可 | `selftest-20260820T160530Z` — expired/consumed / 已过期或已消耗 |
| Runner / runner | `70EAD2CBE6B041C7A0F69FDDE48ADFC192C3E8D2AD4395F028DFB0E6F0BC2BD6` |
| Candidate / 候选 | `4E7041CF2822E510C3C15E0A1E1BF32399164409D5856EEFB6DC4E9D8D031D7B` |
| Result / 结果 | RED before mirror creation / 创建镜像前RED |
| Exact cause / 精确原因 | PowerShell StrictMode encountered mixed tool-schema entries and accessed `$entry.path` where that property was not present. / PowerShell StrictMode 遇到混合工具结构，并在不存在该属性的条目上访问 `$entry.path`。 |

### 7.3 Attempt 2 — pre-mirror timestamp coercion / 第2次——镜像前时间戳强制转换

| Field / 字段 | Exact value / 精确值 |
|---|---|
| Permit / 许可 | `selftest-retry-20260820T161550Z` — expired/consumed / 已过期或已消耗 |
| Runner / runner | `FB6C0FC477F129DE2FAA4532C9333F051948C95F3F281B2711472D8EE4D711E7` |
| Candidate / 候选 | `86A50942DB7E795509D45FC9059936BA65512AD3E50DAC05CB54467FEC30EAFA` |
| Result / 结果 | RED before mirror creation / 创建镜像前RED |
| Exact cause / 精确原因 | `ConvertFrom-Json` converted the ISO timestamp into local `DateTime`, breaking the required literal permit-time interpretation; corrected with `-DateKind String`. / `ConvertFrom-Json` 把 ISO 时间戳转换为本地 `DateTime`，破坏许可时间的字面解释；后以 `-DateKind String` 修正。 |

### 7.4 Attempt 3 — retained denied-probe failure / 第3次——保留的禁入探针失败

| Field / 字段 | Exact value / 精确值 |
|---|---|
| Permit / 许可 | `selftest-retry2-20260820T162827Z` — expired/consumed / 已过期或已消耗 |
| Runner / runner | `05EEAB7089CE784CE0A80AB3BA3FC76CCE280963674B231F3B1980991D96B5E5` |
| Candidate / 候选 | `F7E991504620D9C62A36808ACA380787ECC3C9BC4C04B4D46AED0DDBE08D2190` |
| Retained mirror / 保留镜像 | `C:\Users\asus\r5\bd2902a39655` |
| Result / 结果 | Python SelfTest RED / Python自检RED |
| Exact cause / 精确原因 | `R5_DENIED_PROBE` pointed at mirror source `backend/app/main.py`, which was outside denied roots, so the expected denial did not occur. / `R5_DENIED_PROBE` 指向镜像源码 `backend/app/main.py`，该路径不在禁入根目录内，因此预期拒绝未发生。 |
| Seal / 封印 | `16/16` payloads; source/dependency/Vite before-after pairs equal / 16/16载荷；源码、依赖、Vite前后清单相等 |

### 7.5 Attempt 4 — retained Windows audit-shape failure / 第4次——保留的Windows审计形状失败

| Field / 字段 | Exact value / 精确值 |
|---|---|
| Permit / 许可 | `selftest-retry3-20260820T164159Z` — expired/consumed / 已过期或已消耗 |
| Runner / runner | `53D39E78E5236801512F3BD8AC1B588FE129C72ACD0CB35547A30561FDBB51C2` |
| Candidate / 候选 | `8FC1E534AF4DC96F9A4266502F2C7922DFFAB71C827801A2BB5C82B38034E64A` |
| Retained mirror / 保留镜像 | `C:\Users\asus\r5\7e831062c1d5` |
| Result / 结果 | Python SelfTest RED / Python自检RED |
| Exact cause / 精确原因 | On Windows CPython, the audit event supplied `executable=None` plus a command-line string; the guard attempted `Path(None)` and failed closed. / Windows CPython 审计事件提供 `executable=None` 加命令行字符串；守卫尝试 `Path(None)` 并失败关闭。 |
| Seal / 封印 | `16/16` payloads; source/dependency/Vite before-after pairs equal / 16/16载荷；源码、依赖、Vite前后清单相等 |

### 7.6 Attempt 5 — Python GREEN, Node DNS RED / 第5次——Python GREEN、Node DNS RED

| Field / 字段 | Exact value / 精确值 |
|---|---|
| Permit / 许可 | `selftest-retry4-20260820T172606Z` — expired/consumed / 已过期或已消耗 |
| Runner / runner | `4DDD9390E76483F2B135BC033C08309B5AD8EC6B1E54AB4A6A6CF26BBDB4DB73` |
| Python guard / Python守卫 | `5430AEDF7C215442EA6FB4BAF2D5F143C46598B2EA58C1459F6FD63F8B5C1F07` |
| Candidate / 候选 | `8433F8CCBACF1AE8B87B2C3A868AAD2CC8F700090AB134AE20F81A29FCDAD577` |
| Retained mirror / 保留镜像 | `C:\Users\asus\r5\1c922a2bb656` |
| Python result / Python结果 | GREEN in 5,292 ms; exact 19 denials, 2 PIDs, wrapper-ticket and child-block evidence / 5,292毫秒GREEN；精确19次拒绝、2个PID、wrapper票据与子进程阻断证据 |
| Node result / Node结果 | RED before listener setup / 监听器建立前RED |
| Exact Node cause / 精确Node原因 | Blanket callback `dns.lookup` denial also rejected numeric loopback `127.0.0.1`, which Node internally needed for `Server.listen`. / 全局 callback `dns.lookup` 拒绝同时拒绝 Node `Server.listen` 内部所需的数值回环 `127.0.0.1`。 |
| Seal / 封印 | `21/21` payloads; source/dependency/Vite before-after pairs equal / 21/21载荷；源码、依赖、Vite前后清单相等 |

### 7.7 Current state after Attempt 5 / 第5次之后的当前状态

- **EN:** The Node guard and runner were corrected, producing current candidate `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2`.
  **中文：** Node 守卫与 runner 已修正，形成当前候选 `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2`。
- 📊 **<span style="color:#e67e22">No SelfTest has run after the latest static GREEN; therefore there is no current-candidate runtime verdict or evidence directory.</span>**
- 📊 **<span style="color:#e67e22">最新静态 GREEN 之后没有运行 SelfTest；因此当前候选没有运行时裁定，也没有对应证据目录。</span>**

## 8. Retained evidence and recoverability / 保留证据与可恢复性

### 8.1 Current retained run directories / 当前保留运行目录

| Path / 路径 | Summary truth / 摘要事实 | Seal / 封印 | Recovery boundary / 恢复边界 |
|---|---|---|---|
| `C:\Users\asus\r5\bd2902a39655` | Candidate `F7E991...`; Python probe was not denied; RED. / 候选F7E991…；Python探针未被拒绝；RED。 | 16 payloads / 16载荷 | Retain read-only; `node_modules` detached. / 只读保留；node_modules已解除。 |
| `C:\Users\asus\r5\7e831062c1d5` | Candidate `8FC1E5...`; Windows `executable=None` audit shape; RED. / 候选8FC1E5…；Windows executable=None审计形状；RED。 | 16 payloads / 16载荷 | Retain read-only; `node_modules` detached. / 只读保留；node_modules已解除。 |
| `C:\Users\asus\r5\1c922a2bb656` | Candidate `8433F8...`; Python GREEN, Node numeric-loopback DNS RED. / 候选8433F8…；Python GREEN、Node数值回环DNS RED。 | 21 payloads / 21载荷 | Retain read-only; `node_modules` detached. / 只读保留；node_modules已解除。 |

### 8.2 Historical long-path mirror / 历史长路径镜像

- **EN:** The predecessor referenced `C:\Users\asus\AppData\Local\Temp\radar-r5-isolated-9f96e3d08c0c4a6195d76de54407bdbe` as retained. A read-only execution-time existence check returned **missing**.
  **中文：** 上一交接把 `C:\Users\asus\AppData\Local\Temp\radar-r5-isolated-9f96e3d08c0c4a6195d76de54407bdbe` 记为保留。执行时只读存在性检查返回 **缺失**。
- **EN:** This note does not infer who removed it and does not recreate it. The predecessor’s written result remains historical documentation; current recoverable evidence is limited to the three short-path directories above.
  **中文：** 本文不推断是谁移除，也不重建它。上一交接的书面结果仍是历史记录；当前可恢复证据仅限上表三个短路径目录。
- ⚠️ **<span style="color:#e67e22">Do not delete, mutate, rerun in place, or treat any retained mirror as a deployable build.</span>**
- ⚠️ **<span style="color:#e67e22">不得删除、修改、原地复跑任何保留镜像，也不得把它们当成可部署构建。</span>**

## 9. Current five-file candidate identity / 当前五文件候选身份

### 9.1 Exact execution-time bytes / 执行时精确字节

| Component / 组件 | Absolute path / 绝对路径 | Bytes / 字节 | SHA-256 |
|---|---|---:|---|
| Runner / 运行器 | `C:\Users\asus\r5h\round5-detached-v2.ps1` | 74,157 | `FD000AB6AED2FBB7C11911B0B33A2AECD4250D825C3102A882461D3EAE166E07` |
| Python guard / Python守卫 | `C:\Users\asus\r5h\guards\sitecustomize.py` | 16,683 | `5430AEDF7C215442EA6FB4BAF2D5F143C46598B2EA58C1459F6FD63F8B5C1F07` |
| Node guard / Node守卫 | `C:\Users\asus\r5h\guards\network-guard.cjs` | 16,774 | `C894BE2908D4AB9D6A964ED28BCB3395136C1DE4863DB395B973555A542F393B` |
| Round-5 contract / 第5轮契约 | `C:\Users\asus\r5h\contracts\round5.json` | 11,828 | `C9123400B1C3490AAF345A2A581CFC28F8DEC446AAFB20C66493B02D7D45CDDF` |
| Failed-64 contract / 失败64契约 | `C:\Users\asus\r5h\contracts\failed-64.json` | 5,625 | `9D03C22BC1235242010861F9B5E81B1A5CB5C5B77C29B1FF12B21BD072D7D08A` |

### 9.2 Closure and candidate / 闭包与候选

- **Source closure count / 源码闭包数量:** `337`
- **Source closure digest / 源码闭包摘要:** `B40BDE9A96BD35D89E6D067D60AA5D4CAB2303C5853BA0C426A6039267CFD301`
- **Current candidate identity / 当前候选身份:** `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2`
- **Current physical permit / 当前物理许可:** **None / 无**
- **Current SelfTest evidence / 当前SelfTest证据:** **None; candidate not executed / 无；候选未执行**

🔥 **<span style="color:#e74c3c">Any one-byte change to any of these five files, the closure, or the candidate invalidates the static GREEN and requires a new Director review.</span>**

🔥 **<span style="color:#e74c3c">五个文件、源码闭包或候选中的任意一字节变化都会使静态 GREEN 失效，并要求代码总监重新审查。</span>**

## 10. Latest Node DNS correction / 最新Node DNS修正

### 10.1 Corrected callback policy / 修正后的回调策略

1. **EN:** Captured callback `dns.lookup` permits only exact string hosts `127.0.0.1` and `::1`.
   **中文：** 捕获的 callback `dns.lookup` 只允许精确字符串主机 `127.0.0.1` 与 `::1`。
2. **EN:** An allowed numeric loopback is logged as `allowed-numeric-loopback`, then delegated to the captured original lookup implementation.
   **中文：** 获准的数值回环会记录为 `allowed-numeric-loopback`，随后交给捕获的原始 lookup 实现。
3. **EN:** Every hostname, non-loopback numeric address, missing host, and non-string host is denied.
   **中文：** 所有主机名、非回环数值地址、缺失主机与非字符串主机全部拒绝。
4. **EN:** Every other callback DNS function is denied; every `dns.promises` function is rejected.
   **中文：** 其他所有 callback DNS 函数全部拒绝；所有 `dns.promises` 函数全部拒绝。
5. **EN:** ESM (ECMAScript module) synchronization, same-PID listener ownership, hard-denied ports `3000/8000/8765`, external-target denial, UDP denial, and unregistered-loopback denial remain unchanged.
   **中文：** ESM（ECMAScript 模块）同步、同 PID 监听器归属、硬拒绝端口 `3000/8000/8765`、外部目标拒绝、UDP 拒绝及未登记回环拒绝均保持不变。

### 10.2 Updated SelfTest expectations / 更新后的自检预期

- **EN:** Node SelfTest now explicitly calls `dns.lookup('example.com')` and expects denial operation `dns.lookup` with detail `example.com`.
  **中文：** Node SelfTest 现显式调用 `dns.lookup('example.com')`，并预期拒绝操作为 `dns.lookup`、详情为 `example.com`。
- **EN:** The expected Node denial count is exactly `14`.
  **中文：** Node 预期拒绝计数精确为 `14`。
- **EN:** Python expectations remain exactly `19` denials with the previously proven 2-PID, wrapper-ticket, and child-block evidence.
  **中文：** Python 预期继续精确为 `19` 次拒绝，并保留此前已证明的 2-PID、wrapper 票据与子进程阻断证据。
- 💡 **<span style="color:#2980b9">This is a reviewed design correction. Its runtime behavior remains unknown until the one authorized SelfTest actually completes and seals evidence.</span>**
- 💡 **<span style="color:#2980b9">这是一项已审查的设计修正；在获准的那一次 SelfTest 实际完成并封印证据前，其运行行为仍未知。</span>**

## 11. Code Director verdict and permit state / 代码总监裁定与许可状态

### 11.1 Binding verdict / 约束性裁定

- **EN:** Independent Code Director verdict: **static GREEN for exactly one fresh complete SelfTest only** on the five exact files, closure, and candidate in section 9.
  **中文：** 独立代码总监裁定：仅对第9节五个精确文件、闭包与候选，给予“**只允许一次全新完整 SelfTest**”的静态 GREEN。
- **EN:** The verdict does not state that SelfTest passed; no such run occurred after the correction.
  **中文：** 该裁定不表示 SelfTest 已通过；修正后没有发生这种运行。
- **EN:** Collect64, Preflight64, Full, synthetic manual E2E, production, live/network, database, credentials, services, and canary remain forbidden.
  **中文：** Collect64、Preflight64、Full、合成手工 E2E、生产、真实/联网、数据库、凭据、服务与 canary 全部继续禁止。

### 11.2 Required new physical permit / 必需的新物理许可

A valid future permit must contain all of the following. / 未来有效许可必须同时包含：

- `phase: SelfTest`
- `verdict: GO`
- An ISO expiry later than run start and no more than 15 minutes in the future. / ISO 到期时间晚于运行开始，且最多不超过未来 15 分钟。
- All five exact hashes in section 9. / 第9节全部五个精确哈希。
- Source closure `337` plus digest `B40BDE...D301`. / 源码闭包337及摘要B40BDE…D301。
- Candidate `E1749BE...95C2`. / 候选E1749BE…95C2。
- `prior_artifacts: []`

### 11.3 Old permit inventory / 旧许可清单

| Permit / 许可 | Expiry UTC / UTC到期 | Candidate / 候选 | Current validity / 当前有效性 |
|---|---|---|---|
| `selftest-20260820T160530Z` | `2026-08-20T16:19:00Z` | `4E7041CF...31D7B` | Expired/consumed; wrong bytes / 已过期或已消耗；字节不匹配 |
| `selftest-retry-20260820T161550Z` | `2026-08-20T16:29:00Z` | `86A50942...EAFA` | Expired/consumed; wrong bytes / 已过期或已消耗；字节不匹配 |
| `selftest-retry2-20260820T162827Z` | `2026-08-20T16:40:00Z` | `F7E99150...2190` | Expired/consumed; retained RED run / 已过期或已消耗；保留RED运行 |
| `selftest-retry3-20260820T164159Z` | `2026-08-20T16:53:00Z` | `8FC1E534...E64A` | Expired/consumed; retained RED run / 已过期或已消耗；保留RED运行 |
| `selftest-retry4-20260820T172606Z` | `2026-08-20T17:37:00Z` | `8433F8CC...D577` | Expired/consumed; retained RED run / 已过期或已消耗；保留RED运行 |

⚠️ **<span style="color:#e74c3c">None of the five old permits can authorize the current `E1749...` candidate.</span>**

⚠️ **<span style="color:#e74c3c">五份旧许可均不能授权当前 `E1749...` 候选。</span>**

## 12. Platform defects versus harness-only defects / 平台缺陷与仅门禁缺陷

### 12.1 Proven platform defect / 已证明的平台缺陷

- **EN:** The Link runtime close race was a genuine product-code lifecycle defect. It was fixed only in `link_collection_runtime.py` plus `test_link_collection_api_runtime.py`, frozen at the exact hashes in section 5, and supported by focused evidence.
  **中文：** Link runtime close 竞态是真实产品代码生命周期缺陷。它仅在 `link_collection_runtime.py` 与 `test_link_collection_api_runtime.py` 两文件中修复，按第5节精确哈希冻结，并有专项证据支撑。

### 12.2 Harness-only failures / 仅门禁失败

The following failures belong to the detached test harness, not to platform behavior. / 以下失败属于外置测试门禁，而非平台行为：

- Mixed-schema `$entry.path` access under PowerShell StrictMode. / PowerShell StrictMode 下混合结构的 `$entry.path` 访问。
- ISO timestamp coercion by `ConvertFrom-Json`. / `ConvertFrom-Json` 对 ISO 时间戳的强制转换。
- A denied-file probe located outside the denied roots. / 禁入文件探针位于禁入根目录外。
- Windows CPython `subprocess.Popen` audit arguments with `executable=None`. / Windows CPython `subprocess.Popen` 审计参数中的 `executable=None`。
- Node callback DNS guard denying numeric loopback required by local listener creation. / Node callback DNS 守卫拒绝本地监听器创建所需数值回环。
- Historical 64 long-path mirror failures: 23 path, 31 blanket socket, 10 fixture-name mismatch. / 历史64项长路径镜像失败：23项路径、31项全局socket、10项fixture名称不匹配。

### 12.3 What remains unknown / 仍未知的内容

- 📌 **<span style="color:#2980b9">The current `E1749...` candidate is statically reviewed but unexecuted. It is therefore neither runtime GREEN nor runtime RED.</span>**
- 📌 **<span style="color:#2980b9">当前 `E1749...` 候选已通过静态审查但尚未执行，因此既不是运行时 GREEN，也不是运行时 RED。</span>**
- **EN:** The overall Round-5 gate remains RED because the latest completed run is RED and no clean successor evidence exists.
  **中文：** 第5轮整体门禁继续为 RED，因为最近完成的运行仍为 RED，且不存在干净的后继证据。
- **EN:** Harness-only classification narrows causality; it never converts a failed or unexecuted phase into a pass.
  **中文：** 归类为门禁缺陷只能缩小因果范围，绝不会把失败或未执行阶段改写为通过。

## 13. Ownership and freeze map / 所有权与冻结图

| Owner / 负责人 | Owned scope / 归属范围 | State / 状态 | Must not do / 禁止动作 |
|---|---|---|---|
| Root coordinator / 主协调Agent | Evidence coordination, future Director request, phase sequencing. / 证据协调、未来总监请求、阶段顺序。 | Active coordinator / 活动协调 | Must not self-authorize or call static GREEN a test pass. / 不得自行授权或把静态GREEN称为测试通过。 |
| Platform harness implementation / 平台门禁实现 | Exact five files in section 9 only, when a proven harness defect requires change. / 仅第9节五文件，且只在门禁缺陷被证明时。 | Current bytes frozen pending SelfTest / 当前字节冻结等待自检 | No edit before current one-run opportunity is resolved; any edit requires new identity/review. / 当前单次机会解决前不得修改；任何修改都需新身份/复审。 |
| Independent Code Director / 独立代码总监 | Static review, physical permit, post-run evidence verdict. / 静态审查、物理许可、运行后证据裁定。 | Latest static GREEN limited to one SelfTest / 最新静态GREEN仅限一次SelfTest | Cannot be inferred to approve later phases. / 不得推定其批准后续阶段。 |
| Link Collection / 链接采集 | Exact R1-R4C/P2 source identities in section 5. / 第5节精确R1-R4C/P2源码身份。 | Frozen/no feature edits / 冻结、禁止功能修改 | No reimplementation, cleanup, live URLs, or production. / 禁止重做、清理、真实链接或生产。 |
| Subtitle Direct / 字幕直取 | Exact three source identities in section 4. / 第4节三项精确源码身份。 | Frozen/OFF; capacity moved to Platform / 冻结/OFF；产能转平台 | No subtitle feature development or switch activation. / 禁止字幕功能研发或开启开关。 |
| Frontend redesign / 前端改版 | Navigation, collection, video-assets review slices. / 导航、采集、视频资产复审切片。 | UNFROZEN; collection CSS provenance conflict / 未冻结；采集CSS来源冲突 | No claim of completion and no opportunistic edit during Round-5 gate. / 不得宣称完成，门禁期间不得顺手修改。 |
| Documentation / 文档 | This note plus minimal backlink in prior note. / 本文及上一笔记最小反向链接。 | Complete after verification / 核验后完成 | No code, harness, evidence, test, service, or production action. / 禁止代码、门禁、证据、测试、服务或生产操作。 |

💡 **<span style="color:#2980b9">Freeze means exact bytes plus named evidence and ownership. “Looks complete” is not a freeze.</span>**

💡 **<span style="color:#2980b9">冻结意味着精确字节、指定证据与明确归属；“看起来完成”不等于冻结。</span>**

## 14. Open blockers and unresolved questions / 当前阻断与未决问题

### P0 — One current-candidate SelfTest / 当前候选的一次SelfTest

- **EN:** Obtain a fresh physical permit matching all exact identities and then run exactly one complete SelfTest. Until then, current candidate behavior is unknown.
  **中文：** 必须取得匹配全部精确身份的新物理许可，随后只运行一次完整 SelfTest。在此之前，当前候选行为未知。
- **EN:** The run must create a fresh identity/evidence directory and must not reuse or mutate any retained run directory.
  **中文：** 运行必须创建全新身份/证据目录，不得复用或修改任何保留目录。

### P0 — Separate production authorization / 独立生产授权

- 🔥 **<span style="color:#e74c3c">`PARALLEL_REQUEST_RATE_LIMIT_VERIFIED=False` continues to block simultaneous multi-account production.</span>**
- 🔥 **<span style="color:#e74c3c">`PARALLEL_REQUEST_RATE_LIMIT_VERIFIED=False` 继续阻断多账号同时生产。</span>**
- **EN:** The reviewed MediaCrawler authorization hash set remains empty.
  **中文：** 经审查的 MediaCrawler 授权哈希集合仍为空。
- **EN:** Legal/license approval, ACL (access-control list, defining who may access what), protected identity, credential handling, authorized canary, rollback drill, monitoring, and observability evidence remain absent.
  **中文：** 法律/许可证批准、ACL（访问控制列表，即规定谁能访问什么）、受保护身份、凭据处理、获批 canary、回滚演练、监控与可观测性证据仍缺失。

### P1 — Frontend coherent freeze / 前端统一冻结

- **EN:** Resolve who or what changed `collection-workflow.css` from the prior `9E9E...` identity to current `9808...` bytes.
  **中文：** 需要查清是谁或什么把 `collection-workflow.css` 从旧 `9E9E...` 身份变为当前 `9808...` 字节。
- **EN:** Perform one authorized, coherent visual/responsive review across supported widths for navigation, collection, and video assets, then issue exact hashes.
  **中文：** 对导航、采集、视频资产在支持宽度下执行一次获批的统一视觉/响应式复审，再签发精确哈希。

### Unresolved questions / 未决问题

1. **EN:** Will the independent Director issue a fresh SelfTest permit for current `E1749...` before any byte changes?
   **中文：** 独立代码总监是否会在字节变化前为当前 `E1749...` 签发新 SelfTest 许可？
2. **EN:** If SelfTest is GREEN, which next harness phase will be reviewed and separately permitted? No phase is pre-authorized.
   **中文：** 若 SelfTest GREEN，下一门禁阶段将复审并单独许可哪一个？当前没有任何阶段被预授权。
3. **EN:** What is the provenance of current frontend CSS hash `9808...`, and is it intentional?
   **中文：** 当前前端 CSS 哈希 `9808...` 的来源是什么，是否为有意改动？
4. **EN:** Why is the predecessor’s historical long-path mirror no longer present? Preserve the written history; do not speculate or recreate it.
   **中文：** 为什么上一交接中的历史长路径镜像已不存在？应保留书面历史，不猜测、不重建。
5. **EN:** Who owns the external legal, credential, canary, rollback, and observability approvals required for real production?
   **中文：** 真实生产所需法律、凭据、canary、回滚与可观测性批准由谁负责？

## 15. Manual production and multi-account boundary / 手工生产与多账号边界

### Allowed now / 当前允许

- Read-only review of source, harness bytes, permits, and retained evidence. / 只读审查源码、门禁字节、许可与保留证据。
- Independent Code Director review of the exact current candidate. / 独立代码总监审查精确当前候选。
- Creation of a new short-lived physical permit by the authorized Director. / 由获权总监创建新的短时物理许可。
- Exactly one complete SelfTest only after that exact permit is valid. / 只有精确许可有效后，才能运行精确一次完整 SelfTest。
- Documentation and genuine Obsidian backlinks. / 文档与真实 Obsidian 双向链接。

### Forbidden now / 当前禁止

- 🔥 **<span style="color:#e74c3c">No production deployment, production-based Round-5 test, or live simultaneous multi-account collection.</span>**
- 🔥 **<span style="color:#e74c3c">禁止生产部署、用生产完成第5轮测试或真实多账号同时采集。</span>**
- No real Douyin URLs, real account pages, Cookies, tokens, browser profiles, or credentials. / 禁止真实抖音链接、真实账号页、Cookie、token、浏览器配置或凭据。
- No production/live database, production logs, live ports, services, signer, trust-host change, or network probe. / 禁止生产/真实数据库、生产日志、真实端口、服务、签名器、信任域名修改或联网探针。
- No service stop/restart, canary, feature-flag activation, cleanup, or evidence deletion. / 禁止停止/重启服务、canary、开启功能开关、清理或删除证据。
- No Collect64, Preflight64, Full, or synthetic manual E2E under the current verdict. / 当前裁定下禁止 Collect64、Preflight64、Full 或合成手工 E2E。
- No frontend or product-code edits as a side effect of the harness continuation. / 禁止在门禁接力中顺手修改前端或产品代码。

> [!danger] Production gate / 生产门禁
> **EN:** Even a future SelfTest GREEN would validate only the harness self-check phase. It would not authorize production, manual live testing, or simultaneous multi-account collection.
>
> **中文：** 即使未来 SelfTest GREEN，也只验证门禁自检阶段；它不会授权生产、真实手工测试或多账号同时采集。

## 16. Exact next-account resume sequence / 下一账号精确续接顺序

### 16.1 First read and verify / 首先阅读与核验

1. **EN:** Treat this as relay continuation. Work from `D:\全自动爬取短视频、推文爆款程序`; do not initialize a new project or reset the dirty worktree.
   **中文：** 将本任务视为接力续作。工作目录为 `D:\全自动爬取短视频、推文爆款程序`；不要初始化新项目，也不要重置脏工作树。
2. **EN:** Read this entire note, then follow the genuine in-context link to [[0820下午-开发日志]].
   **中文：** 完整阅读本文，再沿正文真实链接进入 [[0820下午-开发日志]]。
3. **EN:** Verify this note’s SHA-256 from the handoff message before trusting copied values.
   **中文：** 在信任复制值前，先用交接消息中的 SHA-256 核验本文。
4. **EN:** Recompute the five file sizes/hashes, closure count/digest, and candidate. Stop if any value differs.
   **中文：** 重算五文件字节/哈希、闭包数量/摘要及候选。任一值不同就停止。
5. **EN:** Confirm all five old permits remain invalid and that no current permit or unreviewed evidence directory appeared.
   **中文：** 确认五份旧许可继续无效，且没有突然出现当前许可或未经审查的证据目录。

### 16.2 Restore roles without reopening frozen work / 恢复角色但不重开冻结工作

6. **EN:** Assign one Platform harness owner to the exact current five-file candidate only; keep Link and Subtitle sources frozen.
   **中文：** 只为精确当前五文件候选分配一个平台门禁负责人；Link 与字幕源码继续冻结。
7. **EN:** Keep Subtitle Direct capacity reassigned to Platform and all subtitle switches OFF.
   **中文：** 字幕直取产能继续转入平台，所有字幕开关保持 OFF。
8. **EN:** Ask the independent Code Director to re-attest the exact current bytes and issue a fresh SelfTest-only physical permit with at most a 15-minute lifetime and `prior_artifacts: []`.
   **中文：** 请求独立代码总监复核精确当前字节，并签发有效期最多 15 分钟、`prior_artifacts: []` 的仅 SelfTest 新物理许可。

### 16.3 Execute only the authorized phase / 只执行获批阶段

9. **EN:** Under the valid permit, run exactly one complete SelfTest using a fresh short-path mirror/evidence identity. Do not run Collect64, Preflight64, Full, manual, or production phases.
   **中文：** 在有效许可下，使用全新短路径镜像/证据身份运行精确一次完整 SelfTest。不得运行 Collect64、Preflight64、Full、手工或生产阶段。
10. **EN:** Preserve stdout/stderr, guard JSONL, commands, environment, manifests, summary, and seal; verify source/dependency/Vite before-after pairs and link detachment.
    **中文：** 保留 stdout/stderr、守卫 JSONL、命令、环境、清单、摘要与封印；核验源码/依赖/Vite 前后清单及联接解除。
11. **EN:** If RED, keep Round 5 RED, retain the mirror, identify only the proven cause, make only a bounded harness fix, compute a new candidate, and return to Director review.
    **中文：** 若 RED，第5轮继续 RED，保留镜像，只识别已证明原因，只做有界门禁修复，计算新候选，并返回代码总监复审。
12. **EN:** If SelfTest GREEN, freeze that evidence and request an independent Director decision for exactly one next phase. Do not infer permission for later phases.
    **中文：** 若 SelfTest GREEN，冻结该证据，并请求独立代码总监只裁定一个下一阶段。不得推定后续阶段许可。

### 16.4 Release remains separate / 发布仍是独立流程

13. **EN:** Only after all offline phases receive separate clean evidence may a synthetic manual E2E be considered under separate authorization.
    **中文：** 只有所有离线阶段分别取得干净证据后，才可在单独授权下考虑合成手工 E2E。
14. **EN:** Real production still requires independent closure of rate-limit, authorization-hash, legal/license, ACL/identity, credentials, canary, rollback, monitoring, and observability gates.
    **中文：** 真实生产仍必须独立关闭限流、授权哈希、法律/许可证、ACL/身份、凭据、canary、回滚、监控与可观测性门禁。
15. **EN:** Resolve frontend provenance and perform a coherent visual/responsive freeze separately; do not mix it into the harness phase.
    **中文：** 单独解决前端来源并执行统一视觉/响应式冻结；不要把它混入门禁阶段。

> [!important] Copy-ready relay prompt / 可复制接力提示词
> **EN:** Continue the existing relay; do not start a new project. First fully read `F:\积昌的知识库 - 副本\总结好的大纲以及笔记\实习就业\全自动爬取短视频、推文爆款程序\开发日志\0821凌晨-开发日志.md` and follow its real link to [[0820下午-开发日志]]. Current binding state: Round 5 RED; current candidate `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2` is statically GREEN for exactly one fresh complete SelfTest only, but unexecuted and has no valid permit. Verify the five exact hashes/sizes and closure `337/B40BDE...D301`; all five old permits are invalid. Preserve retained mirrors `bd2902a39655`, `7e831062c1d5`, and `1c922a2bb656`. Keep Link R1-R4C/P2 frozen, Subtitle Direct frozen/OFF with capacity assigned to Platform, and frontend UNFROZEN because collection CSS is `9808...` rather than the prior `9E9E...`. Obtain a new Director-issued SelfTest permit before exactly one run. Collect64, Preflight64, Full, manual, live, production, network, DB, credentials, services, canary, and multi-account remain NO-GO.
>
> **中文：** 继续现有接力，不要启动新项目。先完整阅读 `F:\积昌的知识库 - 副本\总结好的大纲以及笔记\实习就业\全自动爬取短视频、推文爆款程序\开发日志\0821凌晨-开发日志.md`，并沿真实链接阅读 [[0820下午-开发日志]]。当前约束状态：第5轮 RED；当前候选 `E1749BE084761E8FED0D99E246791C1FE701CBEA51087092230034803D1395C2` 只获得针对一次全新完整 SelfTest 的静态 GREEN，但尚未执行且没有有效许可。核验五个精确哈希/字节及闭包 `337/B40BDE...D301`；五份旧许可全部无效。保留镜像 `bd2902a39655`、`7e831062c1d5`、`1c922a2bb656`。保持 Link R1-R4C/P2 冻结；字幕直取冻结/OFF 且产能转平台；前端因采集 CSS 为 `9808...` 而非旧 `9E9E...`，继续 UNFROZEN。运行精确一次 SelfTest 前，先取得代码总监新许可。Collect64、Preflight64、Full、手工、真实、生产、网络、DB、凭据、服务、canary 与多账号全部 NO-GO。

## 17. Do not repeat / 禁止重复劳动

- **EN:** Do not re-plan or reimplement Link R1-R4C/P2; continue from exact frozen identities.
  **中文：** 不要重新规划或重做 Link R1-R4C/P2；从精确冻结身份继续。
- **EN:** Do not restore superseded R4C runtime/test identities.
  **中文：** 不要恢复已被替代的 R4C runtime/test 身份。
- **EN:** Do not reopen Subtitle Direct; its capacity remains assigned to Platform and feature switches remain OFF.
  **中文：** 不要重开字幕直取；其产能继续属于平台，功能开关保持 OFF。
- **EN:** Do not rerun old permits, retained directories, or old candidates.
  **中文：** 不要复用旧许可、保留目录或旧候选。
- **EN:** Do not weaken DNS/network policy to “make tests pass”; only exact numeric loopback for the same-PID owned listener is allowed.
  **中文：** 不要为了“让测试通过”而削弱 DNS/网络策略；只允许同 PID 自有监听器所需的精确数值回环。
- **EN:** Do not use hostnames such as `localhost` as a substitute for exact numeric loopback.
  **中文：** 不要用 `localhost` 等主机名替代精确数值回环。
- **EN:** Do not infer a new CSS freeze from current `9808...` bytes; provenance and unified review are missing.
  **中文：** 不要根据当前 `9808...` 字节推定新的 CSS 冻结；来源与统一复审仍缺失。
- **EN:** Do not stop Firefox/backend or touch live logs merely to quiet sentinels.
  **中文：** 不要为了让哨兵安静而停止 Firefox/后端或触碰真实日志。
- **EN:** Do not install new repositories or dependencies; prior approval was exactly zero.
  **中文：** 不要安装新仓库或依赖；此前批准数量精确为零。
- ⚠️ **<span style="color:#e74c3c">Do not use production to finish Round 5.</span>**
- ⚠️ **<span style="color:#e74c3c">禁止用生产来完成第5轮。</span>**

## 18. Glossary / 名词解释

| Term / 术语 | Plain English explanation | 中文白话解释 |
|---|---|---|
| Code-only GREEN / 仅代码GREEN | Named source and focused offline tests passed; live production is not proven. | 指定源码与专项离线测试通过，不证明真实生产。 |
| Static GREEN / 静态GREEN | Review found the exact bytes suitable for one named next action; the action has not run. | 审查认为精确字节适合一个指定后续动作，但该动作尚未运行。 |
| Runtime GREEN / 运行时GREEN | The exact authorized phase actually ran and all required evidence passed. | 精确获批阶段实际运行，且全部所需证据通过。 |
| Frozen identity / 冻结身份 | Exact bytes represented by full SHA-256, candidate identity, evidence, and ownership. | 由完整SHA-256、候选身份、证据与归属表示的精确字节。 |
| Superseded / 已被替代 | Historically valid evidence that no longer identifies the current candidate. | 历史上有效，但已不再代表当前候选的证据。 |
| Harness / 测试门禁 | Temporary runner, guards, paths, fixtures, and evidence around the actual tests. | 包围真实测试的临时runner、守卫、路径、fixture与证据。 |
| SelfTest / 自检 | A phase proving the harness itself enforces isolation correctly before project tests. | 在项目测试前证明门禁自身正确执行隔离的阶段。 |
| Physical permit / 物理许可 | A short-lived file binding one exact phase to exact hashes, closure, candidate, and expiry. | 将一个精确阶段与精确哈希、闭包、候选及到期时间绑定的短时文件。 |
| Source closure / 源码闭包 | The complete frozen set of source files the candidate depends on, counted and hashed as one identity. | 候选依赖的完整冻结源码集合，以数量和整体摘要表示。 |
| Sentinel / 哨兵 | A watched live file or state whose change proves isolation failure. | 被监视的真实文件或状态；变化即证明隔离失败。 |
| Detached mirror / 独立镜像 | A temporary source copy with isolated data, logs, outputs, and evidence. | 数据、日志、输出与证据均隔离的临时源码副本。 |
| Same-PID-owned loopback / 同PID自有回环 | A local endpoint allowed only when the same test process created and owns the listener. | 只有同一测试进程创建并拥有监听器时才允许的本地端点。 |
| NO-GO | A binding decision forbidding the next operational or production action. | 对下一运营或生产动作具有约束力的禁止结论。 |
| Canary / 灰度验证 | A separately authorized small real run with monitoring and rollback. | 单独获批、具备监控与回滚的小规模真实运行。 |

---

> [!danger] Final handoff gate / 最终交接门禁
> 🔥 **<span style="color:#e74c3c">Do not start production, real URLs, manual live testing, or simultaneous multi-account collection from this note. / 不得依据本文启动生产、真实链接、真实手工测试或多账号同时采集。</span>**
>
> **EN:** The next lawful technical action is not “production”; it is a newly permitted, exactly one-time SelfTest of the unchanged `E1749...` candidate, followed by independent evidence review.
>
> **中文：** 下一项合规技术动作不是“生产”，而是在候选 `E1749...` 字节不变的前提下取得新许可并只运行一次 SelfTest，随后由独立方复审证据。
