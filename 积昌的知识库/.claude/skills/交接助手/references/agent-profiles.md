# 智能体习性档案（Agent Profiles）

> 用途：做"给 AI 看的交接文档"（交接助手 skill 模式 B）时，先查本档案，按目标智能体的**习性 + 模型质量**定制文档。**不同 AI 产品能力不同、模型质量不同，用同一份通用模板效果极差**——给强模型的文档可能给弱模型根本接不住。本档案解决"这份文档要写成什么样，那个智能体才接得住"。
>
> 使用步骤：① 确认目标智能体 → ② 判断它的**能力层级（L1/L2/L3）** → ③ 读对应档案 → ④ 按"交接文档定制要点" + "与给其他AI的核心差异"生成 → ⑤ 若目标未收录，按能力层级用「通用」规则。

---

## 0. AI 能力分层总览（先定位，再定制）

> **核心认知：交接文档必须"看菜下饭"。同一个项目，给 Codex（L1）和给 DeepSeek Harness（L3）的文档，从内容密度、自包含程度、指令粒度上都必须不一样。** 判断维度有四条：① 能不能读文件（决定要不要把关键内容贴进正文）；② 能不能跑命令（决定验证方式是自己跑还是让人代跑）；③ 模型强不强 / 上下文大不大（决定指令粒度与结构）；④ 会不会"记岔"（决定要不要证据分级）。
>
> ⚠️ **任务类型是正交维度**：本档案的"能力层级"解决"写给哪个 AI"；**任务类型（代码 / 无代码 / 混合）**另由 SKILL.md「第二步：判断任务类型」决定"写代码视角还是任务物视角"。**无论给哪个 AI，只要是无代码任务（纯生图、写文案、做海报/PPT、剪视频、整理数据、调研、写文档等），都必须去掉"代码完成度/从哪行代码继续/函数·行号定位/跑什么命令验证/代码文件地图"这类代码视角字段**，改用任务物视角（产出物 / 素材输入 / 参数设定 / 使用的工具模型 / 验证=看成品效果）。两者相互独立、都要判断。

| 层级 | 典型代表 | 能读文件 | 能跑命令 | 模型质量 / 上下文 | 交接文档策略 |
|------|---------|:-------:|:-------:|------------------|-------------|
| **L1 自治型** | Claude Code、Codex、Cursor、Windsurf、Gemini CLI | ✅ | ✅ | 强：理解复杂指令、自主多步推理、上下文大 | 给"精确地图 + 可执行命令 + 精确接续位置"；可紧凑但信息必须完整精确，它自己读文件补细节 |
| **L2 半自治型** | 能读文件但推理/上下文受限的智能体 | ✅ | ⚠️ 部分 | 中：能读文件但长上下文或复杂指令吃力 | 给"地图 + 关键内容摘要"：重要文件内容直接贴出，步骤拆细，每个关键节点给验证信号 |
| **L3 对话型** | DeepSeek Harness、Kimi、豆包、文心一言、通义、ChatGPT 网页版 | ❌/❓ | ❌/❓ | 弱到中：主要靠对话记忆、读不到文件、上下文有限、可能记岔 | 给"自包含教材"：关键代码/配置/命令**全部贴进正文**，步骤最小化、每步带验证信号、证据分级（✅确定/🔶推断/❓待确认） |

> ⚠️ **硬性规则：绝不允许把给 AI-A 的文档改个标题就当给 AI-B 的文档。** 每份给 AI 的文档都必须从零按目标 AI 的能力层级 + 习性档案重新生成。判断自己是否偷懒的标尺：**把这份文档递给目标 AI，它能不能不问人、不查环境就照做第一步？**

---

## 1. Claude Code（Anthropic 命令行智能体）— L1 自治型

### 能力画像

| 维度 | 说明 |
|------|------|
| 文件系统 | ✅ 可读写本地文件 |
| 命令执行 | ✅ 可执行 bash / PowerShell、git、python 等 |
| 子代理 | ✅ 可启动多个子代理并行处理大任务 |
| 项目规则 | ✅ 自动读取项目根目录 `CLAUDE.md`（全局规则在用户 `~/.claude/CLAUDE.md`） |
| 上下文加载 | ✅ 支持 `<linked_note>` 关联文档、`<editor_selection>` 选中片段 |
| 模型质量 | 强：能理解复杂指令、自主多步推理、上下文窗口大 |
| 擅长 | 代码开发、重构、调试、多文件修改、读整个代码库 |
| 会话迁移 | 会话可被外部工具延续 |

### 交接文档定制要点

1. **给精确路径 + 可执行命令**：Claude Code 能直接跑，文档里每一步都写成可执行指令（`cd`、`python`、`pytest`、`npm` 等），它拿到就能动手。
2. **建议写 CLAUDE.md**：若项目有必须遵守的规则/约束，在文档里建议（或直接帮它）把关键规则写进项目根目录的 `CLAUDE.md`，它会自动加载并遵守。
3. **文件地图按"入口→配置→产出"排列**：标注每个文件可读、作用；它擅长读代码，可给出"关键函数在哪个文件哪一行"。
4. **下一步计划写成任务列表**：它适合逐项执行；给"先做 A、再做 B"的清单，每项可验证。
5. **验证方法必须可跑**：如"跑 `pytest` 通过 281 条""启动服务后看到 XXX 日志行"。
6. **可提示用子代理**：若任务大（如跨多模块重构），提示它可并行派生子代理。
7. **给它留"提问入口"**：交接文档末尾列出❓项，它遇到不确定会主动确认。
8. **接力总览表按"路径+命令+函数"填"从哪开始"列**：入口列直接给 `文件路径:行号`、`函数名`、可执行命令；验证列给"跑什么命令看到什么输出"。
9. **模型强，指令可以"目标+要点"式**：不必把每个文件内容都贴进正文——告诉它看哪个文件、改哪个函数、达到什么效果即可，它能自己读代码补细节。但**结论、路径、命令必须精确**。

### ★ 与给其他 AI 的文档的核心差异

- **vs DeepSeek Harness（L3）**：Claude Code 版**不需要**把关键代码/配置贴进正文（它能自己读文件）；DeepSeek 版**必须**全贴。Claude Code 版指令可以是"目标+要点"式；DeepSeek 版必须"一步步+每个中间信号"。
- **vs Codex（L1）**：Claude Code 版可用跨平台 bash 风格或 PowerShell 均可，且可以建议写 `CLAUDE.md`、可以提示用子代理；Codex 版则偏 PowerShell + 提醒 `config.toml` + 会话文件迁移。

### 生成交接提示词时（模式 D）——★ 完整专属范本（直接套用生成，禁止拿通用骨架改标题）

> 当用户要"让 Claude Code 基于聊天记录写交接文档"时，产出的提示词**全文按下面的专属范本生成**。这份范本在角色定位、信息收集方式、能力定制、附加提醒上与给 Codex / DeepSeek 的范本**整体不同**，不是同一套骨架换一段话。

```markdown
你正在结束一段工作，现在要把当前工作交接给<交接对象：如「一位没参与过项目的实习生（人）」或「下一个续作智能体 Codex（AI）」>。
请基于我们这段对话的聊天记录，以及你实际能访问的本地文件与运行环境，写一份《<项目名> 交接文档（<给人版 / 给 <智能体名> 版>）》。

接手方<对项目几乎零了解>。你的写稿目标：让接手方读完这份文档后，不靠问任何人、不查任何额外资料，就能——①看懂这项目在做什么、做到哪了；②知道哪些做完了、哪些没做完、为什么没做完；③能照着文档直接动手干第一步；④每一步做完都能自己判断"做得对不对"。

═══════════════════════════════════════
【零、先明确三件事（动笔前必读）】
═══════════════════════════════════════
1. 你的身份：你现在是"交接文档撰写人"，不是普通聊天助手，也不是项目的新开发员。你的唯一产出就是这份交接文档。
2. 文档读者：<接手方>。写的时候始终站在读者角度问自己："这段他能看懂吗？这步他能照做吗？"
3. 你的信息边界：素材 = 这段对话的聊天记录 **+ 你能访问的本地文件/运行环境**。你能读文件、能跑 bash / PowerShell 命令——**凡是聊天记录里没有或含糊的路径、命令、函数、文件状态，你必须自己打开文件核实、跑命令验证**，确保每条路径/命令真实可执行。聊天记录与文件里都找不到的，标注"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·五、动笔前先做「总任务目标 + 当前任务进度」双总结（硬性锚点，写在交接摘要之后）】
═══════════════════════════════════════
动笔写正文前，先输出两块"精确总结"，放在文档开头（交接摘要之后），作为整份文档的锚点；后文所有小节都要能挂回这两块：

**A. 总任务目标总结**（3-5 句话精确概括整个任务的最终目标）：
- 做什么：任务/项目的一句话定义。
- 为谁做：服务对象 / 使用场景。
- 做到什么算成功：验收标准 / 成功定义（可检验，能写数字就写数字）。
- 终极交付物：产出一份什么（交付物清单，含路径）。

**B. 当前任务进度总结**（用"百分比 + 阶段 + 差距"精确给出，对照 A 写）：
- 整体完成度百分比（如"约 70%"），并给出判断依据（哪些证据支撑该百分比）。
- 当前所处阶段 / 里程碑：已走完哪些、正在哪一步、还剩哪些。
- 已完成 / 进行中 / 未开始 三档全量清单。
- 与总目标的差距：还差哪几块才算达到 A 的验收标准。
- 当前阻塞点（如有，逐条列出）。

要求：
- A 对应正文"项目总览与验收"小节，B 对应正文"当前精确进度"小节；0 交接摘要中也各用一两句话点出 A、B 核心。
- "未完成接力棒"每一条都要标注"它服务于总目标的哪一部分 / 补完它之后总进度推进到多少"。
- A 或 B 的任何一项若无法从聊天记录或文件核实，标"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·六、先判断任务类型（代码 / 无代码 / 混合）——硬性，动笔前必须先判断】
动笔前，先判断这段对话所交接的"任务"属于哪一类，并在标题或交接摘要里明确标注：
- **代码任务**：交付物是代码/程序/脚本/工程（开发 App、写爬虫、搭后端、写自动化脚本）。→ 全用"代码视角"：代码完成度、文件:行号/函数名、命令行验证、代码文件地图。
- **无代码任务**：交付物不是代码（纯生图、文案、海报/PPT、剪视频、整理数据、调研、写文档）。→ **去掉一切"代码视角"字段**，改用"任务物视角"：产出物、素材/输入、参数/设定、使用的工具/模型、验证=看成品效果。**绝不要写"代码完成度""从哪行代码继续""跑什么命令"这类子虚乌有的内容**——本任务根本没有代码，写了就是错。
- **混合任务**：既有代码又有非代码（如"做了一个能生成图片的程序"+"生产了 30 张图"）。→ 代码部分用代码视角、非代码部分用任务物视角，在文档里明确区分。

【词表替换（无代码/混合的非代码部分适用）】
- "代码完成度/完成度" → 任务整体完成度（如"约 60%：文案+20 张图完成，还差 10 张"）
- "从哪行代码/哪个函数继续" → 用哪个工具 / 哪个 prompt / 继续哪个素材 / 哪个步骤
- "跑什么命令/测试验证" → 看成品效果 / 对照需求是否达标
- "文件地图/代码文件" → 任务物清单（已产出、素材/输入、参数/设定、使用的工具/模型、参考资源）
- 无代码任务的"验证/完成"一律以**成品效果、需求达标**为准，而非"测试通过/接口返回什么"。
判不准就默认"混合"，但在文档里区分清楚。

═══════════════════════════════════════
【一、必须回答的四个问题（四要素）——缺一不可，尤其 ③④ 不许一笔带过】
═══════════════════════════════════════
① 当前已经做了什么（每条写满 4 字段：做了什么 / 产出物在哪 / 怎么验证有效 / 关键数据与效果）
② 还有什么没做的（每条写满 7 字段：还差什么 / 为什么没做完 / 前置条件 / 优先级 / 做到什么算完成 / 从哪开始 / 验证信号）
③ 接下来要接着做什么（与②一一对应、编号一致）
④ 接下来该怎么做（把③展开成 1-N 步，每步：做什么 → 打开哪个文件/跑哪条命令/找谁 → 看到什么结果算做对 → 卡住看哪个文档/问谁）
> 判优标准：接手方能否不问人、不查东西，照着 ③④ 直接动手第一步？不能 → 继续补。
> 🚫 禁止"一笔带过"：❌"还有几件事没做完，下次继续" ❌"详见之前的记录" ❌"等等/后续再说" ❌"这块比较复杂你自己看"。

═══════════════════════════════════════
【二、必须有一张「A→B 接力总览表」】
═══════════════════════════════════════
| # | A 没做完的（接力棒） | 接手方要做什么 | 从哪开始（文件:行号/函数名/命令/找谁） | 做到什么算完成（怎么验证） |
|---|--------------------|---------------|--------------------------------------|---------------------------|
要求：编号与②③一致；"从哪开始"列具体到能直接下手；"做到什么算完成"列可观察可检验；表末给"接力开工第一步"。

═══════════════════════════════════════
【三、文档结构（按交接对象选一套，逐节完整写出，不能跳节/合并/整节删除）】
═══════════════════════════════════════
- 给人版 → 12 节：①给谁看 ②项目总览（大白话） ③背景来龙去脉 ④术语表 ⑤当前进度（人话） ⑥已完成工作 ⑦A没做完的事 ⑧A→B接力总览 ⑨手把手下一步 ⑩关键决策与踩坑 ⑪风险与求助点 ⑫常用资源清单。每个术语都解释、宁可啰嗦不可简略、人版必须比给AI版更详细。
- 给AI版 → 14 节：0交接摘要 1项目总览与验收 2文件地图 3已完成工作 4当前精确进度 5未完成接力棒 6A→B接力总览 7命令级行动计划 8环境与配置 9数据与状态快照 10关键决策与踩坑 11风险与待确认 12续作启动手册 13按智能体定制附加节。

═══════════════════════════════════════
【四、信息收集方式与真实纪律】
═══════════════════════════════════════
1. 优先基于聊天记录：先把这段对话里的目标、进度、决策、踩坑、文件路径、数据全部提取出来作为主素材。
2. 信息不足时的补救顺序：**先自己读本地文件、跑命令核实**（你能做到）→ 再追问用户 → 仍然缺的，标注"❓待确认"。
3. 真实纪律（硬性）：**你能读文件能跑命令，就有责任把每条路径/命令/函数/状态都核实成真的**——禁止写"大概在 xxx""应该能跑"这种没核实过的入口。拿不准的标 ❓；绝不编造。

═══════════════════════════════════════
【五、写作规范与禁止事项】
═══════════════════════════════════════
1. 宁可多，不可漏；能写数字就写数字（条数/时长/通过率/完成度百分比）。
2. 每条路径给相对路径＋一句话用途；命令给完整可复制版本＋预期输出。
3. 全文用 Markdown；给人版讲大白话讲清"为什么这么选"，给AI版零废话、入口精确。
4. 密钥/token 一律脱敏，值写 <已脱敏>。

═══════════════════════════════════════
【六、按你的能力定制的写作方式（务必照做）】
═══════════════════════════════════════
你（Claude Code）能读本地文件、能跑 bash / PowerShell 命令、能自主多步推理、可派生子代理。所以：
① 文档里直接给**精确路径 + 可执行命令**（bash / PowerShell 均可），**不必贴大段文件内容**——你自己读文件补细节，先把每条路径/命令核实成真的。
② 若项目有必须遵守的规则/约束，建议把关键规则写进项目根目录 `CLAUDE.md`，你会自动加载并遵守。
③ 若任务大（跨多模块重构、批量修改），提示你可并行派生子代理拆解。
④ 接力表"从哪开始"列给 `文件:行号 / 函数名 / 可执行命令`；"做到什么算完成"列给"跑什么命令看到什么输出"。
⑤ 文档末尾留"提问入口"：列出 ❓ 项，你遇到不确定会主动向用户确认，而不是乱猜。

═══════════════════════════════════════
【七、输出要求】
═══════════════════════════════════════
1. 输出一份完整的 Markdown 交接文档，不要只给大纲/要点/节选。
2. 文档开头保留元信息：项目名 / 交接对象 / 交接日期。
3. 文档末尾附一栏"遗留不确定项（❓）汇总"。
4. 保存路径：若用户未指定，直接在回复中输出完整 Markdown 供复制。
5. 交稿前必须对照【八】的自查清单逐条检查，全部通过才算完成。

═══════════════════════════════════════
【八、交稿前自查清单（逐条打勾，缺一不可）】
═══════════════════════════════════════
- [ ] 四要素齐全（①4字段②7字段③对应④1-N步带验证信号）
- [ ] A→B 接力总览表存在，编号与②③一致，表末有"接力开工第一步"
- [ ] 选定的结构（给人12节/给AI14节）每一节都写到了
- [ ] 全文无"等等/下次继续/详见以前记录/后续再说"等一笔带过
- [ ] 无编造：每条路径/命令/结论要么有出处、要么标 ❓
- [ ] **每条路径/命令我已亲自打开文件核实或跑命令验证过，真实可执行**
- [ ] 给人版有术语表/背景/手把手/求助点；给AI版入口精确、有"如何验证续上了"
- [ ] 密钥/token 已脱敏；元信息齐全；末尾附 ❓ 汇总
- [ ] 开头有「总任务目标 + 当前任务进度」双总结（A 目标含验收标准，B 进度含完成度百分比/阶段/差距），且与正文各节逐条对应
- [ ] 判优标准复核：接手方能否不问人、不查资料，照着③④直接动手第一步？
```

---

## 2. Codex（OpenAI 智能体）— L1 自治型

### 能力画像

| 维度 | 说明 |
|------|------|
| 文件系统 | ✅ 可读写本地文件 |
| 命令执行 | ✅ 可执行命令（Windows 环境常用 PowerShell；也支持 bash） |
| 会话记录 | ✅ 本地会话文件 `C:\Users\asus\.codex\sessions\*.jsonl`，可跨账号/机器迁移 |
| 模型配置 | `config.toml` 可配第三方模型（如 deepseek） |
| 网络依赖 | 部分环境依赖代理/分流规则（如 OpenAI 专用节点） |
| 模型质量 | 强：能理解复杂指令、自主多步推理、上下文大 |
| 擅长 | 代码、运维、批量处理、长任务 |

### 交接文档定制要点

1. **给 Windows 风格命令**：PowerShell 为主（`.ps1`、`robocopy`、`tasklist` 等），与它的运行环境匹配。
2. **标注"从哪个函数/哪一行继续"**：Codex 要基于现状改代码，文档须明确当前代码状态和接续位置。
3. **提醒 config.toml**：若项目依赖第三方模型/供应商配置，提醒它检查 `config.toml`，避免模型不可用。
4. **说明会话可迁移性**：若需跨机迁移，提示会话文件位置与迁移方式（拷 sessions 不拷 auth.json）。
5. **验证方法**：跑测试/启动服务/检查输出文件；给出明确成功信号。
6. **网络注意事项**：若项目涉及外网访问，在文档标注代理/分流要求（参考知识库 Codex 网络故障档案）。
7. **明确"当前代码状态"**：Codex 强在续改代码，文档要把"现在代码是什么状态、还差什么"写清楚。
8. **接力总览表按"PowerShell 命令+函数级定位"填"从哪开始"列**：入口列给 `文件.ps1`、`Get-NetTCPConnection`、`.\start.ps1`、函数名/行号；验证列给 `tasklist`、`curl`、测试输出等 Codex 能直接执行的判定信号。
9. **模型强，指令可以"目标+要点"式**：与 Claude Code 类似，路径/函数/命令精确即可，不必贴大段代码；它自己读文件补细节。

### ★ 与给其他 AI 的文档的核心差异

- **vs DeepSeek Harness（L3）**：Codex 版给"精确路径 + PowerShell 命令 + 函数级定位"即可，代码不必全贴（它能读文件）；DeepSeek 版**必须自包含**（关键代码/配置全贴进正文），且要附"用户核对清单"防记岔。同一份内容，Codex 版可以更紧凑，DeepSeek 版必须更"保姆级"。
- **vs Claude Code（L1）**：Codex 版统一用 PowerShell 风格命令、提醒 `config.toml`、说明会话文件可迁移；Claude Code 版跨平台命令即可、建议写 `CLAUDE.md`、提示子代理。

### 生成交接提示词时（模式 D）——★ 完整专属范本（直接套用生成，禁止拿通用骨架改标题）

> 当用户要"让 Codex 基于聊天记录写交接文档"时，产出的提示词**全文按下面的专属范本生成**。这份范本在角色定位、信息收集方式、能力定制、附加提醒上与给 Claude Code / DeepSeek 的范本**整体不同**——尤其强调 Windows/PowerShell 环境、config.toml、会话迁移，而不是 Claude Code 的 CLAUDE.md/子代理。

```markdown
你正在结束一段工作，现在要把当前工作交接给<交接对象：如「一位完全没参与过项目的实习生（人）」或「下一个续作智能体（AI）」>。
请基于我们这段对话的聊天记录，以及你实际能访问的本地文件与运行环境（Windows / PowerShell），写一份《<项目名> 交接文档（<给人版 / 给 <智能体名> 版>）》。

接手方<对项目几乎零了解>。你的写稿目标：让接手方读完这份文档后，不靠问任何人、不查任何额外资料，就能——①看懂这项目在做什么、做到哪了；②知道哪些做完了、哪些没做完、为什么没做完；③能照着文档直接动手干第一步；④每一步做完都能自己判断"做得对不对"。

═══════════════════════════════════════
【零、先明确三件事（动笔前必读）】
═══════════════════════════════════════
1. 你的身份：你现在是"交接文档撰写人"，不是普通聊天助手，也不是项目的新开发员。你的唯一产出就是这份交接文档。
2. 文档读者：<接手方>。写的时候始终站在读者角度问自己："这段他能看懂吗？这步他能照做吗？"
3. 你的信息边界：素材 = 这段对话的聊天记录 **+ 你能访问的本地文件/运行环境**。你能读文件、能跑 PowerShell 命令——**凡是聊天记录里没有或含糊的路径、命令、函数、文件状态，你必须自己打开文件核实、跑命令验证**，确保每条路径/命令真实可执行。聊天记录与文件里都找不到的，标注"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·五、动笔前先做「总任务目标 + 当前任务进度」双总结（硬性锚点，写在交接摘要之后）】
═══════════════════════════════════════
动笔写正文前，先输出两块"精确总结"，放在文档开头（交接摘要之后），作为整份文档的锚点；后文所有小节都要能挂回这两块：

**A. 总任务目标总结**（3-5 句话精确概括整个任务的最终目标）：
- 做什么：任务/项目的一句话定义。
- 为谁做：服务对象 / 使用场景。
- 做到什么算成功：验收标准 / 成功定义（可检验，能写数字就写数字）。
- 终极交付物：产出一份什么（交付物清单，含路径）。

**B. 当前任务进度总结**（用"百分比 + 阶段 + 差距"精确给出，对照 A 写）：
- 整体完成度百分比（如"约 70%"），并给出判断依据（哪些证据支撑该百分比）。
- 当前所处阶段 / 里程碑：已走完哪些、正在哪一步、还剩哪些。
- 已完成 / 进行中 / 未开始 三档全量清单。
- 与总目标的差距：还差哪几块才算达到 A 的验收标准。
- 当前阻塞点（如有，逐条列出）。

要求：
- A 对应正文"项目总览与验收"小节，B 对应正文"当前精确进度"小节；0 交接摘要中也各用一两句话点出 A、B 核心。
- "未完成接力棒"每一条都要标注"它服务于总目标的哪一部分 / 补完它之后总进度推进到多少"。
- A 或 B 的任何一项若无法从聊天记录或文件核实，标"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·六、先判断任务类型（代码 / 无代码 / 混合）——硬性，动笔前必须先判断】
动笔前，先判断这段对话所交接的"任务"属于哪一类，并在标题或交接摘要里明确标注：
- **代码任务**：交付物是代码/程序/脚本/工程（开发 App、写爬虫、搭后端、写自动化脚本）。→ 全用"代码视角"：代码完成度、文件:行号/函数名、命令行验证、代码文件地图。
- **无代码任务**：交付物不是代码（纯生图、文案、海报/PPT、剪视频、整理数据、调研、写文档）。→ **去掉一切"代码视角"字段**，改用"任务物视角"：产出物、素材/输入、参数/设定、使用的工具/模型、验证=看成品效果。**绝不要写"代码完成度""从哪行代码继续""跑什么命令"这类子虚乌有的内容**——本任务根本没有代码，写了就是错。
- **混合任务**：既有代码又有非代码（如"做了一个能生成图片的程序"+"生产了 30 张图"）。→ 代码部分用代码视角、非代码部分用任务物视角，在文档里明确区分。

【词表替换（无代码/混合的非代码部分适用）】
- "代码完成度/完成度" → 任务整体完成度（如"约 60%：文案+20 张图完成，还差 10 张"）
- "从哪行代码/哪个函数继续" → 用哪个工具 / 哪个 prompt / 继续哪个素材 / 哪个步骤
- "跑什么命令/测试验证" → 看成品效果 / 对照需求是否达标
- "文件地图/代码文件" → 任务物清单（已产出、素材/输入、参数/设定、使用的工具/模型、参考资源）
- 无代码任务的"验证/完成"一律以**成品效果、需求达标**为准，而非"测试通过/接口返回什么"。
判不准就默认"混合"，但在文档里区分清楚。

═══════════════════════════════════════
【一、必须回答的四个问题（四要素）——缺一不可，尤其 ③④ 不许一笔带过】
═══════════════════════════════════════
① 当前已经做了什么（每条写满 4 字段：做了什么 / 产出物在哪 / 怎么验证有效 / 关键数据与效果）
② 还有什么没做的（每条写满 7 字段：还差什么 / 为什么没做完 / 前置条件 / 优先级 / 做到什么算完成 / 从哪开始 / 验证信号）
③ 接下来要接着做什么（与②一一对应、编号一致）
④ 接下来该怎么做（把③展开成 1-N 步，每步：做什么 → 打开哪个文件/跑哪条命令/找谁 → 看到什么结果算做对 → 卡住看哪个文档/问谁）
> 判优标准：接手方能否不问人、不查东西，照着 ③④ 直接动手第一步？不能 → 继续补。
> 🚫 禁止"一笔带过"：❌"还有几件事没做完，下次继续" ❌"详见之前的记录" ❌"等等/后续再说" ❌"这块比较复杂你自己看"。

═══════════════════════════════════════
【二、必须有一张「A→B 接力总览表」】
═══════════════════════════════════════
| # | A 没做完的（接力棒） | 接手方要做什么 | 从哪开始（文件:行号/函数名/PowerShell命令/找谁） | 做到什么算完成（怎么验证） |
|---|--------------------|---------------|-----------------------------------------------|---------------------------|
要求：编号与②③一致；"从哪开始"列用 **PowerShell 命令 + 函数级定位**（如 `.\start.ps1`、`Get-NetTCPConnection`、`函数名`）；"做到什么算完成"列给你能直接执行的判定信号（`tasklist` / `curl` / 测试输出）；表末给"接力开工第一步"。

═══════════════════════════════════════
【三、文档结构（按交接对象选一套，逐节完整写出，不能跳节/合并/整节删除）】
═══════════════════════════════════════
- 给人版 → 12 节：①给谁看 ②项目总览（大白话） ③背景来龙去脉 ④术语表 ⑤当前进度（人话） ⑥已完成工作 ⑦A没做完的事 ⑧A→B接力总览 ⑨手把手下一步 ⑩关键决策与踩坑 ⑪风险与求助点 ⑫常用资源清单。每个术语都解释、宁可啰嗦不可简略、人版必须比给AI版更详细。
- 给AI版 → 14 节：0交接摘要 1项目总览与验收 2文件地图 3已完成工作 4当前精确进度 5未完成接力棒 6A→B接力总览 7命令级行动计划 8环境与配置 9数据与状态快照 10关键决策与踩坑 11风险与待确认 12续作启动手册 13按智能体定制附加节。

═══════════════════════════════════════
【四、信息收集方式与真实纪律】
═══════════════════════════════════════
1. 优先基于聊天记录：先把这段对话里的目标、进度、决策、踩坑、文件路径、数据全部提取出来作为主素材。
2. 信息不足时的补救顺序：**先自己读本地文件、跑 PowerShell 命令核实**（你能做到）→ 再追问用户 → 仍然缺的，标注"❓待确认"。
3. 真实纪律（硬性）：**你既然能跑 PowerShell，就有责任把每条命令实际执行一遍确认可用**——禁止写"应该能跑"这种未验证的命令。拿不准的标 ❓；绝不编造。

═══════════════════════════════════════
【五、写作规范与禁止事项】
═══════════════════════════════════════
1. 宁可多，不可漏；能写数字就写数字（条数/时长/通过率/完成度百分比）。
2. 每条路径给相对路径＋一句话用途；命令给完整可复制版本＋预期输出。
3. 全文用 Markdown；给人版讲大白话讲清"为什么这么选"，给AI版零废话、入口精确。
4. 密钥/token 一律脱敏，值写 <已脱敏>。

═══════════════════════════════════════
【六、按你的能力定制的写作方式（务必照做）】
═══════════════════════════════════════
你（Codex）能读本地文件、能跑 PowerShell 命令、能自主多步推理。所以：
① 文档里直接给 **Windows/PowerShell 风格命令**（`.ps1`、`robocopy`、`tasklist`、`Get-NetTCPConnection` 等），你自己执行验证，**不必贴大段代码**——给 `文件:行号 / 函数名 / 可执行命令` 即可。
② 交接点定位到**函数/行号**：明确"从哪个函数/哪一行/哪个文件继续"，便于你在现有代码上续改。
③ 若项目依赖第三方模型/供应商配置，提醒检查 `config.toml`（如 DeepSeek 接入 OpenAI 的配置）；若涉及外网访问，注意代理/分流要求。
④ 若需跨机迁移，说明会话文件位置与迁移方式（拷 sessions 不拷 auth.json）。
⑤ 接力表"从哪开始"列给 `PowerShell 命令 + 函数级定位`；"做到什么算完成"列给 `tasklist / curl / 测试输出` 等你能直接执行的判定信号。

═══════════════════════════════════════
【七、输出要求】
═══════════════════════════════════════
1. 输出一份完整的 Markdown 交接文档，不要只给大纲/要点/节选。
2. 文档开头保留元信息：项目名 / 交接对象 / 交接日期。
3. 文档末尾附一栏"遗留不确定项（❓）汇总"。
4. 保存路径：若用户未指定，直接在回复中输出完整 Markdown 供复制。
5. 交稿前必须对照【八】的自查清单逐条检查，全部通过才算完成。

═══════════════════════════════════════
【八、交稿前自查清单（逐条打勾，缺一不可）】
═══════════════════════════════════════
- [ ] 四要素齐全（①4字段②7字段③对应④1-N步带验证信号）
- [ ] A→B 接力总览表存在，编号与②③一致，表末有"接力开工第一步"
- [ ] 选定的结构（给人12节/给AI14节）每一节都写到了
- [ ] 全文无"等等/下次继续/详见以前记录/后续再说"等一笔带过
- [ ] 无编造：每条路径/命令/结论要么有出处、要么标 ❓
- [ ] **每条 PowerShell 命令我已实际执行验证过，真实可运行；每个函数/行号我已打开文件核实存在**
- [ ] 给人版有术语表/背景/手把手/求助点；给AI版入口精确、有"如何验证续上了"
- [ ] 涉及 config.toml/代理/会话迁移的提醒已写入对应小节
- [ ] 密钥/token 已脱敏；元信息齐全；末尾附 ❓ 汇总
- [ ] 开头有「总任务目标 + 当前任务进度」双总结（A 目标含验收标准，B 进度含完成度百分比/阶段/差距），且与正文各节逐条对应
- [ ] 判优标准复核：接手方能否不问人、不查资料，照着③④直接动手第一步？
```

---

## 3. DeepSeek Harness（DeepSeek 智能体）— L3 对话型

### 能力画像

| 维度 | 说明 |
|------|------|
| 文件系统 | ❓ 有限/不确定：主要靠对话上下文，可能无法可靠读取本地文件 |
| 命令执行 | ❓ 不确定：一般不执行本地命令 |
| 会话记录 | ❌ 无本地会话文件可读（不像 Codex 有 sessions） |
| 上下文窗口 | ⚠️ 有限：早期内容可能遗忘 |
| 模型质量 | 中偏弱：复杂指令或超长上下文容易"接不住"或"记岔" |
| 擅长 | 对话式分析、内容生成、基于对话记忆的续作 |

### 交接文档定制要点

1. **自包含（最重要，且宁可多写不可省略）**：关键代码、配置、命令**直接粘贴进文档正文**，不要只给路径——它可能读不到文件。**注意：详细 ≠ 只给结论；每个未完成项都要按七要素写满（还差什么/为什么/前置/优先级/完成定义/从哪开始/验证信号），只是"从哪开始"要贴内容而不是只写路径。**
2. **证据分级**：每条信息标注 ✅确定 / 🔶推断 / ❓待确认——它会"记岔"，分级后用户能识别哪些要复核。
3. **附"给用户的核对清单"**：它看不到文件系统，写的路径可能记错；文档末尾附一栏"请用户核对以下路径/状态是否属实"，由人代跑验证。
4. **结构清晰、关键在前、密度高但不省略**：它上下文有限，把**最关键信息（项目现状 + 接力棒 + 下一步第一步）放最前**；后续细节按需给，但**每条细节本身要写透**，宁可让文档变长，也不准用"等等/后续再议"一笔带过。
5. **给交接摘要放最前**：500 字启动包让它快速进入状态，再按需读全文。
6. **验证方式靠"人代跑"**：它不能自己执行命令，所以每个验证信号要写成"请用户跑 `XXX`，看到 `YYY` 输出即成功"。
7. **步骤最小化**：每步只做一件事，每步结束都问"是否完成"或给一个可观察信号，降低它"一次理解太多"的负担。
8. **接力总览表必须"自包含"**："从哪开始"列不能只写路径——把关键文件内容/代码片段直接贴进对应行；"做到什么算完成"列给"用户帮跑什么命令、看到什么输出"。
9. **不要让它猜**：任何可能产生歧义的地方（路径、函数名、命令、验收口径）都直接写出来，不给它脑补的空间。

### ★ 与给其他 AI 的文档的核心差异

- **vs Codex / Claude Code（L1）**：给 L1 的文档给"精确路径 + 命令"它自己就能读、能跑；给 DeepSeek 的文档**必须把内容本身贴进正文**，验证方式从"你自己跑命令"变成"用户帮你跑命令"。**同一份内容，DeepSeek 版的指令粒度要细得多——几乎每一行都要写到"照着念就能执行"。**
- **模型质量差异**：Codex 能一次吸收"目标+要点"并自行展开；DeepSeek 更容易在长指令或含糊指令上跑偏，所以文档要把歧义消掉、把步骤拆小、把验证信号写死。

### 生成交接提示词时（模式 D）——★ 完整专属范本（直接套用生成，禁止拿通用骨架改标题）

> 当用户要"让 DeepSeek Harness 基于聊天记录写交接文档"时，产出的提示词**全文按下面的专属范本生成**。这份范本与给 Codex / Claude Code 的范本**整体不同**——核心差异：DeepSeek 读不到文件、不能跑命令、会记岔，所以信息收集只能靠聊天记录，验证靠人代跑，必须证据分级 + 用户核对清单；而 L1 版（Codex/Claude Code）是自己读文件、自己跑命令验证。

```markdown
你正在结束一段工作，现在要把当前工作交接给<交接对象：如「下一个续作智能体 Codex（AI）」或「一位没参与过项目的实习生（人）」>。
请基于我们这段对话的聊天记录，写一份《<项目名> 交接文档（<给人版 / 给 <智能体名> 版>）》。

接手方<对项目几乎零了解>。你的写稿目标：让接手方读完这份文档后，不靠问任何人、不查任何额外资料，就能——①看懂这项目在做什么、做到哪了；②知道哪些做完了、哪些没做完、为什么没做完；③能照着文档直接动手干第一步；④每一步做完都能自己判断"做得对不对"。

═══════════════════════════════════════
【零、先明确三件事（动笔前必读）】
═══════════════════════════════════════
1. 你的身份：你现在是"交接文档撰写人"，不是普通聊天助手，也不是项目的新开发员。你的唯一产出就是这份交接文档。
2. 文档读者：<接手方>。写的时候始终站在读者角度问自己："这段他能看懂吗？这步他能照做吗？"
3. 你的信息边界：素材 = 这段对话的聊天记录。**你主要靠对话记忆，可能读不到本地文件、不能跑命令、上下文有限、容易记岔。** 所以：**聊天记录里没有或记不清的，绝不脑补**——要么追问用户，要么标注"❓待确认"写进核对清单，绝不确定地写一个你可能记错的路径/命令/数字。

═══════════════════════════════════════
【〇·五、动笔前先做「总任务目标 + 当前任务进度」双总结（硬性锚点，写在交接摘要之后）】
═══════════════════════════════════════
动笔写正文前，先输出两块"精确总结"，放在文档开头（交接摘要之后），作为整份文档的锚点；后文所有小节都要能挂回这两块：

**A. 总任务目标总结**（3-5 句话精确概括整个任务的最终目标）：
- 做什么：任务/项目的一句话定义。
- 为谁做：服务对象 / 使用场景。
- 做到什么算成功：验收标准 / 成功定义（可检验，能写数字就写数字）。
- 终极交付物：产出一份什么（交付物清单，含路径）。

**B. 当前任务进度总结**（用"百分比 + 阶段 + 差距"精确给出，对照 A 写）：
- 整体完成度百分比（如"约 70%"），并给出判断依据（哪些证据支撑该百分比）。
- 当前所处阶段 / 里程碑：已走完哪些、正在哪一步、还剩哪些。
- 已完成 / 进行中 / 未开始 三档全量清单。
- 与总目标的差距：还差哪几块才算达到 A 的验收标准。
- 当前阻塞点（如有，逐条列出）。

要求：
- A 对应正文"项目总览与验收"小节，B 对应正文"当前精确进度"小节；0 交接摘要中也各用一两句话点出 A、B 核心。
- "未完成接力棒"每一条都要标注"它服务于总目标的哪一部分 / 补完它之后总进度推进到多少"。
- A 或 B 的任何一项若无法从聊天记录或文件核实，标"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·六、先判断任务类型（代码 / 无代码 / 混合）——硬性，动笔前必须先判断】
动笔前，先判断这段对话所交接的"任务"属于哪一类，并在标题或交接摘要里明确标注：
- **代码任务**：交付物是代码/程序/脚本/工程（开发 App、写爬虫、搭后端、写自动化脚本）。→ 全用"代码视角"：代码完成度、文件:行号/函数名、命令行验证、代码文件地图。
- **无代码任务**：交付物不是代码（纯生图、文案、海报/PPT、剪视频、整理数据、调研、写文档）。→ **去掉一切"代码视角"字段**，改用"任务物视角"：产出物、素材/输入、参数/设定、使用的工具/模型、验证=看成品效果。**绝不要写"代码完成度""从哪行代码继续""跑什么命令"这类子虚乌有的内容**——本任务根本没有代码，写了就是错。
- **混合任务**：既有代码又有非代码（如"做了一个能生成图片的程序"+"生产了 30 张图"）。→ 代码部分用代码视角、非代码部分用任务物视角，在文档里明确区分。

【词表替换（无代码/混合的非代码部分适用）】
- "代码完成度/完成度" → 任务整体完成度（如"约 60%：文案+20 张图完成，还差 10 张"）
- "从哪行代码/哪个函数继续" → 用哪个工具 / 哪个 prompt / 继续哪个素材 / 哪个步骤
- "跑什么命令/测试验证" → 看成品效果 / 对照需求是否达标
- "文件地图/代码文件" → 任务物清单（已产出、素材/输入、参数/设定、使用的工具/模型、参考资源）
- 无代码任务的"验证/完成"一律以**成品效果、需求达标**为准，而非"测试通过/接口返回什么"。
判不准就默认"混合"，但在文档里区分清楚。

═══════════════════════════════════════
【一、必须回答的四个问题（四要素）——缺一不可，尤其 ③④ 不许一笔带过】
═══════════════════════════════════════
① 当前已经做了什么（每条写满 4 字段：做了什么 / 产出物在哪 / 怎么验证有效 / 关键数据与效果）
② 还有什么没做的（每条写满 7 字段：还差什么 / 为什么没做完 / 前置条件 / 优先级 / 做到什么算完成 / 从哪开始 / 验证信号）
③ 接下来要接着做什么（与②一一对应、编号一致）
④ 接下来该怎么做（把③展开成 1-N 步，每步：做什么 → 打开哪个文件/跑哪条命令/找谁 → 看到什么结果算做对 → 卡住看哪个文档/问谁）
> 判优标准：接手方能否不问人、不查东西，照着 ③④ 直接动手第一步？不能 → 继续补。
> 🚫 禁止"一笔带过"：❌"还有几件事没做完，下次继续" ❌"详见之前的记录" ❌"等等/后续再说" ❌"这块比较复杂你自己看"。

═══════════════════════════════════════
【二、必须有一张「A→B 接力总览表」】
═══════════════════════════════════════
| # | A 没做完的（接力棒） | 接手方要做什么 | 从哪开始（贴出关键内容/代码片段，不只写路径） | 做到什么算完成（用户帮跑什么命令、看到什么输出） |
|---|--------------------|---------------|---------------------------------------------|-----------------------------------------------|
要求：编号与②③一致；**"从哪开始"列不能只写路径——把你记得的关键文件内容/代码片段直接贴进这一列**，并标证据分级；"做到什么算完成"列给"用户帮跑什么命令、看到什么输出"；表末给"接力开工第一步"。

═══════════════════════════════════════
【三、文档结构（按交接对象选一套，逐节完整写出，不能跳节/合并/整节删除）】
═══════════════════════════════════════
- 给人版 → 12 节：①给谁看 ②项目总览（大白话） ③背景来龙去脉 ④术语表 ⑤当前进度（人话） ⑥已完成工作 ⑦A没做完的事 ⑧A→B接力总览 ⑨手把手下一步 ⑩关键决策与踩坑 ⑪风险与求助点 ⑫常用资源清单。每个术语都解释、宁可啰嗦不可简略、人版必须比给AI版更详细。
- 给AI版 → 14 节：0交接摘要 1项目总览与验收 2文件地图 3已完成工作 4当前精确进度 5未完成接力棒 6A→B接力总览 7命令级行动计划 8环境与配置 9数据与状态快照 10关键决策与踩坑 11风险与待确认 12续作启动手册 13按智能体定制附加节。
- **自包含要求贯穿所有节**：凡是涉及路径、代码、配置、命令的地方，都要把内容本身贴出来，绝不只写"在 xxx 文件里"。

═══════════════════════════════════════
【四、信息收集方式与真实纪律】
═══════════════════════════════════════
1. 优先基于聊天记录：先把这段对话里的目标、进度、决策、踩坑、文件路径、数据全部提取出来作为主素材。
2. 信息不足时的补救顺序：**先追问用户**（你读不到文件）→ 仍然缺的，标注"❓待确认"，写进文档并在末尾的核对清单里请用户补。
3. 真实纪律（硬性）：**你读不到文件、不能跑命令，所以凡是路径、命令、函数、状态，必须标证据分级**：✅确定（聊天里明确出现）/ 🔶推断（你推理补的）/ ❓待确认（记不清/没明说）。**禁止把推断写成确定**；绝不编造。

═══════════════════════════════════════
【五、写作规范与禁止事项】
═══════════════════════════════════════
1. 宁可多，不可漏；能写数字就写数字（条数/时长/通过率/完成度百分比）。
2. 每条路径给相对路径＋一句话用途；命令给完整可复制版本＋预期输出。
3. 全文用 Markdown；给人版讲大白话讲清"为什么这么选"，给AI版零废话、入口精确。
4. 密钥/token 一律脱敏，值写 <已脱敏>。

═══════════════════════════════════════
【六、按你的能力定制的写作方式（务必照做）】
═══════════════════════════════════════
你（DeepSeek Harness）主要靠这段对话的聊天记录，可能读不到本地文件、不能跑命令。所以：
① **自包含**：关键代码、配置、命令、路径、数据一律直接贴进文档正文，绝不只写"在 xxx 文件里"——你连自己写的路径都可能记错，要给就给全。
② **验证靠人代跑**：文档里每个验证步骤写成"请用户代跑 XXX，看到 YYY 输出即成功"，因为你自己不能跑命令。
③ **证据分级**：每条信息标注 ✅确定 / 🔶推断 / ❓待确认，绝不装确定。
④ **每步只做一件事**，每步给一个可观察信号；把歧义全部消掉，不要让你猜（不写"等""后续再说"）。
⑤ 文档末尾必附 **"请用户核对的清单"**：把你提到的所有路径/文件状态/命令逐条列出，请用户核对是否属实并代跑确认，修正后再交付接手方。

═══════════════════════════════════════
【七、输出要求】
═══════════════════════════════════════
1. 输出一份完整的 Markdown 交接文档，不要只给大纲/要点/节选。
2. 文档开头保留元信息：项目名 / 交接对象 / 交接日期。
3. 文档末尾附一栏"遗留不确定项（❓）汇总"，**以及"请用户核对的清单"**。
4. 保存路径：若用户未指定，直接在回复中输出完整 Markdown 供复制。
5. 交稿前必须对照【八】的自查清单逐条检查，全部通过才算完成。

═══════════════════════════════════════
【八、交稿前自查清单（逐条打勾，缺一不可）】
═══════════════════════════════════════
- [ ] 四要素齐全（①4字段②7字段③对应④1-N步带验证信号）
- [ ] A→B 接力总览表存在，编号与②③一致，表末有"接力开工第一步"
- [ ] 选定的结构（给人12节/给AI14节）每一节都写到了
- [ ] 全文无"等等/下次继续/详见以前记录/后续再说"等一笔带过
- [ ] 无编造：每条路径/命令/结论要么有出处、要么标 ❓
- [ ] **关键内容已自包含贴进正文（不只给路径）；每条信息已标证据分级 ✅/🔶/❓**
- [ ] **验证步骤已写成"请用户代跑"，且末尾附"请用户核对的清单"**
- [ ] 给人版有术语表/背景/手把手/求助点；给AI版入口精确、有"如何验证续上了"
- [ ] 密钥/token 已脱敏；元信息齐全；末尾附 ❓ 汇总
- [ ] 开头有「总任务目标 + 当前任务进度」双总结（A 目标含验收标准，B 进度含完成度百分比/阶段/差距），且与正文各节逐条对应
- [ ] 判优标准复核：接手方能否不问人、不查资料，照着③④直接动手第一步？
```

---

## 4. 通用网页版对话 AI（Kimi / 豆包 / 文心一言 / 通义 / ChatGPT 网页版等）— L3 对话型

### 能力画像

| 维度 | 说明 |
|------|------|
| 文件系统 | ❌ 通常读不到本地文件（除非用户手动粘贴文件内容） |
| 命令执行 | ❌ 一般不执行本地命令 |
| 会话记录 | ❌ 无本地会话文件 |
| 上下文窗口 | ⚠️ 因产品而异，普遍有限 |
| 模型质量 | 中偏弱：适合对话式续作，复杂多步任务需更细引导 |
| 擅长 | 内容生成、基于对话记忆的分析与续写 |

### 交接文档定制要点

1. **默认按 L3 对话型处理**：自包含、贴内容、步骤小、验证靠人代跑、证据分级（✅/🔶/❓）。参考 DeepSeek Harness 档案的 9 条，全部适用。
2. **首次交接先给"上下文包"**：因为读不到文件，把项目核心背景 + 关键文件内容压缩成一段"给它读的上下文"，随文档一起给。
3. **让用户参与**：这类 AI 通常需要用户在对话框里贴文档、贴文件内容、代跑命令，所以文档要额外标注"哪些部分需要用户手动粘贴/代跑"。

### ★ 与给其他 AI 的文档的核心差异

- **vs L1（Claude Code/Codex）**：L1 能自己读文件跑命令，文档给"地图+命令"；这类网页 AI 给"地图"也读不到，**必须把地图上的每个关键文件内容直接贴给它**，并明确"请用户代跑以下命令"。

### 生成交接提示词时（模式 D）——★ 完整专属范本（直接套用生成，禁止拿通用骨架改标题）

> 当用户要"让 Kimi / 豆包 / 文心一言 / 通义 / ChatGPT 网页版等基于聊天记录写交接文档"时，产出的提示词**全文按下面的专属范本生成**。与 DeepSeek 范本相比，额外多了两条差异：**① 首次交接先给"上下文包"**（用户会把文件内容粘贴给你）；**② 明确列出"需要用户提供哪些文件内容 / 代跑哪些命令"**。

```markdown
你正在结束一段工作，现在要把当前工作交接给<交接对象：如「一位没参与过项目的实习生（人）」或「下一个续作智能体（AI）」>。
请基于我们这段对话的聊天记录，写一份《<项目名> 交接文档（<给人版 / 给 <智能体名> 版>）》。

接手方<对项目几乎零了解>。你的写稿目标：让接手方读完这份文档后，不靠问任何人、不查任何额外资料，就能——①看懂这项目在做什么、做到哪了；②知道哪些做完了、哪些没做完、为什么没做完；③能照着文档直接动手干第一步；④每一步做完都能自己判断"做得对不对"。

═══════════════════════════════════════
【零、先明确三件事（动笔前必读）】
═══════════════════════════════════════
1. 你的身份：你现在是"交接文档撰写人"，不是普通聊天助手，也不是项目的新开发员。你的唯一产出就是这份交接文档。
2. 文档读者：<接手方>。写的时候始终站在读者角度问自己："这段他能看懂吗？这步他能照做吗？"
3. 你的信息边界：素材 = 这段对话的聊天记录。**你读不到本地文件、不能跑命令。** 聊天记录里没有的，你需要请用户手动粘贴文件内容给你、或代跑命令确认；无法核实的标注"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·五、动笔前先做「总任务目标 + 当前任务进度」双总结（硬性锚点，写在交接摘要之后）】
═══════════════════════════════════════
动笔写正文前，先输出两块"精确总结"，放在文档开头（交接摘要之后），作为整份文档的锚点；后文所有小节都要能挂回这两块：

**A. 总任务目标总结**（3-5 句话精确概括整个任务的最终目标）：
- 做什么：任务/项目的一句话定义。
- 为谁做：服务对象 / 使用场景。
- 做到什么算成功：验收标准 / 成功定义（可检验，能写数字就写数字）。
- 终极交付物：产出一份什么（交付物清单，含路径）。

**B. 当前任务进度总结**（用"百分比 + 阶段 + 差距"精确给出，对照 A 写）：
- 整体完成度百分比（如"约 70%"），并给出判断依据（哪些证据支撑该百分比）。
- 当前所处阶段 / 里程碑：已走完哪些、正在哪一步、还剩哪些。
- 已完成 / 进行中 / 未开始 三档全量清单。
- 与总目标的差距：还差哪几块才算达到 A 的验收标准。
- 当前阻塞点（如有，逐条列出）。

要求：
- A 对应正文"项目总览与验收"小节，B 对应正文"当前精确进度"小节；0 交接摘要中也各用一两句话点出 A、B 核心。
- "未完成接力棒"每一条都要标注"它服务于总目标的哪一部分 / 补完它之后总进度推进到多少"。
- A 或 B 的任何一项若无法从聊天记录或文件核实，标"❓待确认"，绝不编造。

═══════════════════════════════════════
【〇·六、先判断任务类型（代码 / 无代码 / 混合）——硬性，动笔前必须先判断】
动笔前，先判断这段对话所交接的"任务"属于哪一类，并在标题或交接摘要里明确标注：
- **代码任务**：交付物是代码/程序/脚本/工程（开发 App、写爬虫、搭后端、写自动化脚本）。→ 全用"代码视角"：代码完成度、文件:行号/函数名、命令行验证、代码文件地图。
- **无代码任务**：交付物不是代码（纯生图、文案、海报/PPT、剪视频、整理数据、调研、写文档）。→ **去掉一切"代码视角"字段**，改用"任务物视角"：产出物、素材/输入、参数/设定、使用的工具/模型、验证=看成品效果。**绝不要写"代码完成度""从哪行代码继续""跑什么命令"这类子虚乌有的内容**——本任务根本没有代码，写了就是错。
- **混合任务**：既有代码又有非代码（如"做了一个能生成图片的程序"+"生产了 30 张图"）。→ 代码部分用代码视角、非代码部分用任务物视角，在文档里明确区分。

【词表替换（无代码/混合的非代码部分适用）】
- "代码完成度/完成度" → 任务整体完成度（如"约 60%：文案+20 张图完成，还差 10 张"）
- "从哪行代码/哪个函数继续" → 用哪个工具 / 哪个 prompt / 继续哪个素材 / 哪个步骤
- "跑什么命令/测试验证" → 看成品效果 / 对照需求是否达标
- "文件地图/代码文件" → 任务物清单（已产出、素材/输入、参数/设定、使用的工具/模型、参考资源）
- 无代码任务的"验证/完成"一律以**成品效果、需求达标**为准，而非"测试通过/接口返回什么"。
判不准就默认"混合"，但在文档里区分清楚。

═══════════════════════════════════════
【一、必须回答的四个问题（四要素）——缺一不可，尤其 ③④ 不许一笔带过】
═══════════════════════════════════════
① 当前已经做了什么（每条写满 4 字段：做了什么 / 产出物在哪 / 怎么验证有效 / 关键数据与效果）
② 还有什么没做的（每条写满 7 字段：还差什么 / 为什么没做完 / 前置条件 / 优先级 / 做到什么算完成 / 从哪开始 / 验证信号）
③ 接下来要接着做什么（与②一一对应、编号一致）
④ 接下来该怎么做（把③展开成 1-N 步，每步：做什么 → 打开哪个文件/跑哪条命令/找谁 → 看到什么结果算做对 → 卡住看哪个文档/问谁）
> 判优标准：接手方能否不问人、不查东西，照着 ③④ 直接动手第一步？不能 → 继续补。
> 🚫 禁止"一笔带过"：❌"还有几件事没做完，下次继续" ❌"详见之前的记录" ❌"等等/后续再说" ❌"这块比较复杂你自己看"。

═══════════════════════════════════════
【二、必须有一张「A→B 接力总览表」】
═══════════════════════════════════════
| # | A 没做完的（接力棒） | 接手方要做什么 | 从哪开始（贴出关键内容/代码片段，不只写路径） | 做到什么算完成（用户帮跑什么命令、看到什么输出） |
|---|--------------------|---------------|---------------------------------------------|-----------------------------------------------|
要求：编号与②③一致；"从哪开始"列不能只写路径——把关键内容/代码片段直接贴进这一列；"做到什么算完成"列给"用户帮跑什么命令、看到什么输出"；表末给"接力开工第一步"。

═══════════════════════════════════════
【三、文档结构（按交接对象选一套，逐节完整写出，不能跳节/合并/整节删除）】
═══════════════════════════════════════
- 给人版 → 12 节：①给谁看 ②项目总览（大白话） ③背景来龙去脉 ④术语表 ⑤当前进度（人话） ⑥已完成工作 ⑦A没做完的事 ⑧A→B接力总览 ⑨手把手下一步 ⑩关键决策与踩坑 ⑪风险与求助点 ⑫常用资源清单。每个术语都解释、宁可啰嗦不可简略、人版必须比给AI版更详细。
- 给AI版 → 14 节：0交接摘要 1项目总览与验收 2文件地图 3已完成工作 4当前精确进度 5未完成接力棒 6A→B接力总览 7命令级行动计划 8环境与配置 9数据与状态快照 10关键决策与踩坑 11风险与待确认 12续作启动手册 13按智能体定制附加节。
- 自包含要求贯穿所有节：凡是涉及路径、代码、配置、命令的地方，都要把内容本身贴出来，绝不只写"在 xxx 文件里"。

═══════════════════════════════════════
【四、信息收集方式与真实纪律】
═══════════════════════════════════════
1. 优先基于聊天记录：先把这段对话里的目标、进度、决策、踩坑、文件路径、数据全部提取出来作为主素材。
2. 信息不足时的补救顺序：**明确列出"需要用户提供哪些文件内容 / 代跑哪些命令"** → 用户提供后再补 → 仍缺的标注"❓待确认"。
3. 真实纪律（硬性）：你读不到文件，所以凡是路径、命令、函数、状态，必须标证据分级：✅确定 / 🔶推断 / ❓待确认；**禁止把推断写成确定**；绝不编造。

═══════════════════════════════════════
【五、写作规范与禁止事项】
═══════════════════════════════════════
1. 宁可多，不可漏；能写数字就写数字（条数/时长/通过率/完成度百分比）。
2. 每条路径给相对路径＋一句话用途；命令给完整可复制版本＋预期输出。
3. 全文用 Markdown；给人版讲大白话讲清"为什么这么选"，给AI版零废话、入口精确。
4. 密钥/token 一律脱敏，值写 <已脱敏>。

═══════════════════════════════════════
【六、按你的能力定制的写作方式（务必照做）】
═══════════════════════════════════════
你（Kimi / 豆包 / 文心 / 通义 / ChatGPT 网页版等）读不到本地文件、不能跑命令。所以：
① **自包含**：关键代码、配置、命令、路径、数据一律直接贴进文档正文，绝不只写"在 xxx 文件里"。
② **先给"上下文包"**：把项目核心背景 + 关键文件内容压缩成一段"给接手方读的上下文"，放在文档开头附近。
③ **明确标注"哪些部分需要用户手动粘贴/代跑"**：请用户把聊天记录之外的关键文件内容粘贴给你，并列出"需要用户提供哪些文件内容 / 代跑哪些命令"的清单。
④ **验证靠人代跑**：每个验证步骤写成"请用户代跑 XXX，看到 YYY 输出即成功"。
⑤ **证据分级**：每条信息标注 ✅确定 / 🔶推断 / ❓待确认，绝不装确定。
⑥ 每步只做一件事，每步给一个可观察信号；把歧义全部消掉，不要让你猜。

═══════════════════════════════════════
【七、输出要求】
═══════════════════════════════════════
1. 输出一份完整的 Markdown 交接文档，不要只给大纲/要点/节选。
2. 文档开头保留元信息：项目名 / 交接对象 / 交接日期。
3. 文档末尾附一栏"遗留不确定项（❓）汇总"，**以及"请用户核对的清单"（含需要用户粘贴/代跑的内容）**。
4. 保存路径：若用户未指定，直接在回复中输出完整 Markdown 供复制。
5. 交稿前必须对照【八】的自查清单逐条检查，全部通过才算完成。

═══════════════════════════════════════
【八、交稿前自查清单（逐条打勾，缺一不可）】
═══════════════════════════════════════
- [ ] 四要素齐全（①4字段②7字段③对应④1-N步带验证信号）
- [ ] A→B 接力总览表存在，编号与②③一致，表末有"接力开工第一步"
- [ ] 选定的结构（给人12节/给AI14节）每一节都写到了
- [ ] 全文无"等等/下次继续/详见以前记录/后续再说"等一笔带过
- [ ] 无编造：每条路径/命令/结论要么有出处、要么标 ❓
- [ ] 关键内容已自包含贴进正文；已提供"上下文包"
- [ ] 已列出"需要用户提供哪些文件内容 / 代跑哪些命令"的清单
- [ ] 验证步骤已写成"请用户代跑"，且末尾附"请用户核对的清单"
- [ ] 密钥/token 已脱敏；元信息齐全；末尾附 ❓ 汇总
- [ ] 开头有「总任务目标 + 当前任务进度」双总结（A 目标含验收标准，B 进度含完成度百分比/阶段/差距），且与正文各节逐条对应
- [ ] 判优标准复核：接手方能否不问人、不查资料，照着③④直接动手第一步？
```

---

## 5. 通用 AI 智能体（未收录的产品，先按能力层级套用）

### 判定方法

目标产品不在上面清单里时，先回答四个问题判断它属于哪一层：
1. 它能读取本地文件系统吗？→ 能=L1/L2；不能/不确定=L3
2. 它能执行本地命令吗？→ 能=L1；不能/不确定=L3
3. 它的模型强不强、上下文大不大？→ 强=L1 策略可紧凑；弱=L3 策略要保姆级
4. 它会"记岔"吗？→ 会→必须证据分级

### 交接文档定制要点

1. **先定层，再定制**：按判定结果落到 L1 / L2 / L3 的对应策略。
2. **自包含兜底**：拿不准就默认 L3，关键内容直接贴出。
3. **含验证方法**：必须有"如何验证续上了"的信号；不能自跑就让用户代跑。
4. **标注不确定处**：每条 ❓ 让接手方知道哪些需人工确认。
5. **给交接摘要**：开头的启动包适用于所有智能体。
6. **接力总览表自包含**："从哪开始"列贴内容，"做到什么算完成"列给可观察信号。

**生成交接提示词时（模式 D）**：先按"判定方法"定层，再**直接套用对应层级厂家的完整专属范本**（L1 → 用 Claude Code 或 Codex 的范本之一；L3 → 用 DeepSeek 或通用网页 AI 的范本之一），把范本里的目标智能体名替换掉即可。**禁止自创通用骨架**。拿不准就默认按 L3（DeepSeek/通用网页AI范本）写，因为自包含+人代跑+证据分级在任何情况下都不会错。

---

## 速查对比表

| 智能体 | 层级 | 文件系统 | 跑命令 | 会话可迁移 | 关键定制动作 | 生成提示词时（模式 D） |
|--------|:----:|:--------:|:------:|:----------:|-------------|----------------------|
| Claude Code | L1 | ✅ | ✅ | ✅ | 路径+命令、建议写 CLAUDE.md、子代理、可跑验证；**接力表入口=文件:行号/函数/命令**；指令可"目标+要点"式 | ★**有完整专属范本**（见上）：自己读文件核实、给 bash/PowerShell 命令、写进 CLAUDE.md、可派生子代理、留提问入口 |
| Codex | L1 | ✅ | ✅ | ✅（sessions） | PowerShell 命令、从哪个函数继续、查 config.toml、代理注意；**接力表入口=PowerShell命令+函数级定位**；指令可"目标+要点"式 | ★**有完整专属范本**（见上）：自己跑 PowerShell 验证、PowerShell 命令+函数级定位、查 config.toml、会话迁移（拷 sessions 不拷 auth.json）、代理/分流 |
| DeepSeek Harness | L3 | ❓ | ❓ | ❌ | 自包含**贴内容**、证据分级、附用户核对清单、人代跑验证、步骤最小化；**接力表必须自包含贴内容**；宁可长不可省略 | ★**有完整专属范本**（见上）：贴代码配置进正文、验证靠人代跑、✅/🔶/❓分级、附用户核对清单、每步只做一件事 |
| 通用网页 AI | L3 | ❌ | ❌ | ❌ | 按 L3 处理，另加"上下文包"与"哪些部分需用户粘贴/代跑"标注 | ★**有完整专属范本**（见上）：自包含贴内容 + 上下文包 + 列出"需用户粘贴/代跑"清单 + 人代跑验证 + 证据分级 |
| 通用 AI（未收录） | 先定层 | ❓ | ❓ | ❓ | 按能力层级套用；拿不准默认 L3 自包含 | 定层后**直接套用对应层级厂家的完整专属范本**（L1→Claude Code/Codex 范本，L3→DeepSeek/通用网页AI 范本）并替换目标智能体名；拿不准默认 DeepSeek 范本 |

---

## 附：同项目给不同 AI 的文档差异示例（一眼看懂"适配"是什么）

> 以下为同一句交接内容的两种写法，展示 L1 与 L3 的差异：

| 交接点 | 给 Codex（L1）的写法 | 给 DeepSeek Harness（L3）的写法 |
|--------|--------------------|--------------------------------|
| 指出接续位置 | "续写 `backend/app/main.py` 的 `lifespan()`（第 271 行）" | "请打开 `backend/app/main.py` 第 271 行附近，那里有一个 `asyncio.create_task(_run_temp_media_janitor(...))`，你接下来要在它旁边新增一个调度任务。关键代码贴给你：`（贴 271 行前后代码）`" |
| 给出下一步 | "在 `main.py` 新增 `_run_collection_scheduler` 协程并在 `lifespan()` 注册" | "你要做三小步：① 复制下面这段模板代码……② 把里面 `xxx` 改成 `yyy`……③ 在 271 行后面插入 `asyncio.create_task(...)`。做完后请用户运行 `python -c "...检查..."`，看到 `True` 才算完成。" |
| 验证续上 | "跑 `pytest`，全绿即成功" | "请用户在你的机器上运行 `.\\.venv\\Scripts\\python.exe -m pytest -q`，把输出贴给你；看到 `X passed` 就说明环境是好的。" |
